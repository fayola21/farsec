<title>Issue 32207 -   chromium -    The CLD (Compact Language Detection) code is run in the browser  it should run in the renderer. - Project Hosting on Google Code</title> <pre> The CLD code used to determine the language of a page is run in the browser.  It should run in the renderer so it is sanboxed.    Note: that code is triggered by an extension API. The popular Translate  extension uses that API on every visited page. </pre>