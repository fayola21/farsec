<title>Issue 7942 -   chromium -    SCons enhancement: detect number of CPUs on POSIX (TODO(evanm)) - Project Hosting on Google Code</title> <pre> In src/build/SConscript.main  there is a linux-specific thing that is easy  to change to posix:    elif root_env['PLATFORM'] in ['linux'  'linux2'  'posix']:    # TODO(evanm): this is Linux-specific  not posix.    # Parse /proc/cpuinfo for processor count.    cpus = len([l for l in open('/proc/cpuinfo') if l.startswith('processort')])    This can be rewritten to use the posix api:  cpus = os.sysconf('SC_NPROCESSORS_ONLN')   </pre>