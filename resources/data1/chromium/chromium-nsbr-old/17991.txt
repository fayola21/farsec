<title>Issue 17991 -   chromium -    Mac SSL code doesn't handle case writing to the network while writing is occurring - Project Hosting on Google Code</title> <pre> Repro-ed with a site heavily using SSL.    The Mac SSL code lies to the caller about writes  buffers the data  and  writes in the background. If a second write comes in  the SSL code doesn't  properly handle it  starts a second concurrent write  and the underlying  TCP socket code DCHECKs.   </pre>