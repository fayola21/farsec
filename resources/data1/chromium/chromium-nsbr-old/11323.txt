<title>Issue 11323 -   chromium -    Get rid of the static ProxyConfig::next_id_ so ProxyConfig can be used on multiple threads  - Project Hosting on Google Code</title> <pre> Should remove the static id counter which is used by the constructor  and   instead move the ID assignment logic into ProxyService.    See &lt;<a href="http://codereview.chromium.org/99126">http://codereview.chromium.org/99126</a>&gt; for why this might be used on   another thread. </pre>