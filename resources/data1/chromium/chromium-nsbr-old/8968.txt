<title>Issue 8968 -   chromium -    Implement recursive watches in directory_watcher_inotify.cc - Project Hosting on Google Code</title> <pre> I'll just paste a comment from directory_watcher_inotify.cc here:    // TODO(phajdan.jr): Support recursive watches.  // Unfortunately inotify has no &quot;native&quot; support for them  but it can be  // emulated by walking the directory tree and setting up watches for each  // directory. Of course this is ineffective for large directory trees.  // For the algorithm  see the link below:  // <a href="http://osdir.com/ml/gnome.dashboard.devel/2004-10/msg00022.html">http://osdir.com/ml/gnome.dashboard.devel/2004-10/msg00022.html</a>   </pre>