<title>Issue 2437 -   chromium -    Links clicked which point to open application instances open a new tab instead - Project Hosting on Google Code</title>   1. Create application shortcuts to two Google apps from Chrome  e.g. Gmail    Google Calendar  2. Open both applications  3. From the top of one of the open Google apps/single-window Chrome   instances  click on a link  Calendar for example  for another currently   open Gears app  4. A new tab will open in a Chrome shell      I would expect that if I have something open as an application that this   could be recognized from other instances  and thus focus placed on the   already opened application rather than a new browser tab created.        A new browser tab is opened instead of the already-opened app getting   focus.    Please provide