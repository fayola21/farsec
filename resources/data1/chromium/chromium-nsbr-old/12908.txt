<title>Issue 12908 -   chromium -    Assert clicking &quot;labels&quot; dropdown in gmail - Project Hosting on Google Code</title> <pre> 1. Go to gmail.com and sign in  takes you to the inbox  2. Select a thread by clicking the checkbox  3. Click the &quot;Labels&quot; dropdown in the top bar.    Expected:  - labels dropdown    Actual:  - Tab crashes. In debug builds  it displays this:    ASSERTION FAILED: startPosition.isNotNull() &amp;&amp; endPosition.isNotNull()  (/Users/pinkerton/src/trunk/src/webkit/../third_party/WebKit/WebCore/rendering/RenderTextControl.cpp:251  void WebCore::RenderTextControl::setSelectionRange(int  int))    Not sure if this happens on all platforms  so just filing mac for now  but  I imagine it does.    This makes it very difficult to use gmail with a debug build for dogfooding.    Build from <a href="/p/chromium/source/detail?r=17187">r17187</a>.   </pre>