<title>Issue 1178 -   chromium -    Browser window should not scroll when Java applet has focus - Project Hosting on Google Code</title>   1. Goto a site with a page long enough to have a scroll bar and a java   applet that takes mouse scroll wheel input ( Sample site: ufl.transloc-  inc.com )  2. Use scroll wheel up and down when mouse focus is on (mouse is over) java   applet.      Only the java applet should process the mouse scroll input; the scroll   input is processed only ONCE.      The entire web page scrolls AND the java applet responds to the mouse   scroll input; the scroll input is processed TWICE (by the browser and the   java applet).    Please provide