<title>Issue 20932 -   chromium -    Omnibox does not use the correct search engine to run searches when using custom search engines. - Project Hosting on Google Code</title>   1. create a new search engine with a keyword like &quot;f&quot; and name &quot;foo&quot; (this   is just an example)  2. in the omnibox type [f bar]   3. notice the UI shows &quot;Search foo: f&quot;  4. press enter and notice that it used the default search engine (google)   instead of the actual foo search engine.       searching with a custom search engine directly from the omnibox.      searching is using the default search engine (google) and not using the   custom search engine although it is displaying as if it would use it.    <b>Please provide