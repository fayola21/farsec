<title>Issue 14819 -   chromium -    With --load-extension  Content script can't be injected into page - Project Hosting on Google Code</title> <pre>   1.write any extension who contain content script  2.use --load-extension to load it        content script should be injected into page  but it's not.  This is caused by we choose not to scan scripts if ExtensionService is ready.       </pre>