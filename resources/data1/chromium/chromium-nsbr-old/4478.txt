<title>Issue 4478 -   chromium -    Allow a way for Greasemonkey scripts to be injected earlier than DOMContentLoaded - Project Hosting on Google Code</title> <pre> It is a long standing request to allow injection of Greasemonkey scripts   before any HTML is parsed. WebKit has a hook for this that we can use  we   just need to invent a way for authors to opt-in. It could be something   like:    @inject document-start </pre>