<title>Issue 14871 -   chromium -    User scripts applied to URL that's loaded in an iframe throws errors - Project Hosting on Google Code</title>   1. Create a userscript that applies to a certain URL/page.  2. Create a page that loads this URL/page in an iframe.  3. Navigate to the page containing the iframe in Chromium.      Userscript runs on iframe just as it would if you had navigated to the page   inside the iframe (as the iframe is the URL where the script is being   applied).      When you load the page with the userscript running  it throws security   errors (depending on what you're doing). For instance  if you have jQuery   in the script it throws an error: NO_MODIFICATION_ALLOWED_ERR: DOM   Exception 7. It seems like it believes I'm trying to manipulate the   elements in the iframe from outside the iframe.    <b>Please provide