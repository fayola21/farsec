<title>Issue 16065 -   chromium -    Should show thrumbnail frame with keyboard navigation - Project Hosting on Google Code</title> <pre>   As we show  the thumbnail's frame (window containing pin and remove buttons   on mouse hover  we should also show the frame on accessing them thr keyboard   with tabbing.    Right now  we show empty space instead of frame's title bar as shown in the   screenshot.    We should implement this for tabbing also. </pre>