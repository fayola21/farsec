<title>Issue 12268 -   chromium -    Linux: investigate RenderThemeChromiumLinux::setDefaultFontSize - Project Hosting on Google Code</title> <pre> In webview_impl.cc:    #if defined(OS_WIN)    // RenderTheme is a singleton that needs to know the default font size to    // draw some form controls.  We let it know each time the size changes.    WebCore::RenderThemeChromiumWin::setDefaultFontSize(        preferences.default_font_size);  #endif    Perhaps we should do something similar on linux? It warrants investigation  anyway. Filing a bug so we don't forget. </pre>