<title>Issue 15885 -   chromium -    We need a presubmit check that checks DEPS - Project Hosting on Google Code</title> <pre> We need a presubmit check that checks DEPS...at least the webkit deps    ideally all.  When deps are rolled into the future  the bots will have to do   a full sync on the next cycle  which takes forever. </pre>