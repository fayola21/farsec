<title>Issue 12871 -   chromium -    Properties added to a function object are ignored. - Project Hosting on Google Code</title>   1. Here's a simple test case. Create an HTML file with  the following contents:    &lt;html&gt;    &lt;body&gt;      &lt;script&gt;        var foo = function() {};        foo.name = &quot;hello&quot;;        alert(foo.name);      &lt;/script&gt;    &lt;/body&gt;  &lt;/html&gt;    2. Load the file in chromium.        Once the page is loaded  the alert dialog  should contain the word &quot;hello&quot;.        The alert dialog is empty.    <b>Please provide