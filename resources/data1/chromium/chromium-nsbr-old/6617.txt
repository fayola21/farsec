<title>Issue 6617 -   chromium -    Dragging to bookmarks bar should not make it the default folder - Project Hosting on Google Code</title> <pre> Chrome Version: 1.0.154.43 (not the latest  sorry)      1. Add bookmarks to folder &quot;foo&quot;  2. Observe that &quot;foo&quot; is now the default folder  3. Drag something to the bookmarks toolbar  4. Check the default folder by pressing ctrl-D      &quot;foo&quot; should remain the default destination      &quot;Bookmarks bar&quot; has become the default.  And you usually don't notice until   the bar is full of junk. </pre>