<title>Issue 14878 -   chromium -    Update new tab page tab string formatting - Project Hosting on Google Code</title> <pre> In the new tab js we have something like this:    function formatTabsText(numTabs) {    if (numTabs == 1)      return localStrings.getString('closedwindowsingle');    // TODO(arv): Update grd file to use %s so we can use formatString    // return localStrings.formatString('closedwindowmultiple'  numTabs);    return localStrings.getString('closedwindowmultiple').replace(/%/    numTabs);  }    We should just change the grd file to use %s so we do not have to duplicate   code. </pre>