<title>Issue 2796 -   chromium -    Firefox importer should fallback to browser.search.defaultengine if selectedEngine is empty - Project Hosting on Google Code</title> <pre> Our Firefox importer looks for the value browser.search.selectedEngine to   determine which engine to make the default. 'selectedEngine' is empty if   the user has chosen the default. If selectedEngine is empty we should look   for 'browser.search.defaultengine' to determine the default. </pre>