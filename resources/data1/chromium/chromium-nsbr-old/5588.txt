<title>Issue 5588 -   chromium -    The Chrome installer does not run itself as an administrator (on Windows Vista)  so it can't set itself as the default browser. - Project Hosting on Google Code</title>   1. Install Google Chrome on Windows Vista and select the option to make   Chrome the default browser.          Chrome becoming the default browser.        It doesn't set itself as the default browser until you run it as   administrator.      Please provide