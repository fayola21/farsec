<title>Issue 11962 -   chromium -    Linux: find bar match count should have different background colors - Project Hosting on Google Code</title> <pre> Currently the match count text on Linux uses the background color for the   match count text. Instead  we should use the edit control background color.    The background color (and the foreground color so it can be read) should be   changed when there are no results available. Windows uses light red. </pre>