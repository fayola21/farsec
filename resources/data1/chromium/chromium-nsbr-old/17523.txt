<title>Issue 17523 -   chromium -    Content scripts don't see their own functions in setTimeout - Project Hosting on Google Code</title>   1. Create an extension with a content script which a function foo()  and  contains a call to setTimeout(&quot;foo()&quot;  100);  2. Go to a page which matches that content script      foo is called 100ms later      Uncaught ReferenceError: foo is not defined    <b>Please provide