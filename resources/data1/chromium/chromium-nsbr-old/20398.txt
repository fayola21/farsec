<title>Issue 20398 -   chromium -    PageAction icon disappears on reader.google.com when url includes reference - Project Hosting on Google Code</title> <pre>     1. Create a page action for reader.google.com  2. The page action enables the icon for      <a href="http://www.google.com/reader/view/">http://www.google.com/reader/view/</a>  ... but then navigates to      <a href="http://www.google.com/reader/view/#foo">http://www.google.com/reader/view/#foo</a>  3. Chrome then compares the two URLs and think that the page action icon   should not be visible.  4. The extension never sees this navigation  so it can't reenable the page   action icon on the new URL.    We should ignore the reference (fragment) when comparing the two URLs (for   the purpose of clearing the icons in the Omnibox). </pre>