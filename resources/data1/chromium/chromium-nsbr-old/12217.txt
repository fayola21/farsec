<title>Issue 12217 -   chromium -    bookmarks.onBookmarkRemoved does not return id of removed item - Project Hosting on Google Code</title> <pre> Steps to reproduce:    1. Add a listener to chrome.bookmarks.onBookmarkRemoved  2. Delete a bookmark    Expected result:    I expect to get a js object with the bookmark id and the parentId.    Actual result:    The js object only has the parentId.    It is important to get the bookmark id because otherwise I have to   invalidate the whole folder which is not only inefficient but it makes it   harder to keep track of the selection.      3. The listener </pre>