<title>Issue 8720 -   chromium -    REGRESSION STABLE - uploads &quot;in the background&quot; (dispatched from an iframe) not working - Project Hosting on Google Code</title>   1. Create a page with 2 iframes  one nested inside the other  2. In the main page  create a form with some inputs  including a type=file   one  3. Try to clone the form of the master page inside the last nested iframe   using document.importNode()      a) the cloned form is exactly the same as the original one  b) the cloned form has the submit function defined      a) the cloned form does not have a submit function defined    <b>Please provide