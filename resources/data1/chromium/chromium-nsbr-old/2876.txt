<title>Issue 2876 -   chromium -    Deleting &lt;input&gt; elements from a contentEditable fields causes various errors. - Project Hosting on Google Code</title>   1. Load the attached html file and follow the instruction at the top        Hitting backspace with the caret immediately after an &lt;input&gt; element   should result only in the &lt;input&gt; element being removed from the DOM.        Deleting the button cases a &lt;div style=&quot;text-align: center;&quot;&gt; to be   inserted around the text before the button.     Deleting the other input elements causes a &lt;br/&gt; to be inserted in place of   the deleted &lt;input&gt; element.    Please provide