<title>Issue 7819 -   chromium -    AJAX response is not cached in some cases - Project Hosting on Google Code</title>     Reload the page with the following script.      var xhr1 = new XMLHttpRequest();    xhr1.open('GET'  'test.png'  true);    xhr1.send(null);      window.setTimeout(function() {      var xhr2 = new XMLHttpRequest();      xhr2.open('GET'  'test.png'  true);      xhr2.send(null);    }  2000);        Chrome sends the request only once.        Chrome sends the request twice.    Please provide