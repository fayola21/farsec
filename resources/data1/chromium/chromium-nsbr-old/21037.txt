<title>Issue 21037 -   chromium -    No event is thrown if mole is opened or closed from the toolstrip handle - Project Hosting on Google Code</title>   1. Try do develop an extension that has a mole or use   chrome.toolstrip.expand / collapse to control the mole  2. The user click on the toolstrip handle and the mole opens / collapses        An event (onToolstripCollapse / onToolstripExpanded) is thrown so the   extension is able to handle it.      No event is thrown.    <b>Please provide