<title>Issue 3432 -   chromium -    Hovering the mouse over the lock/warning icon shows an empty bubble - Project Hosting on Google Code</title> <pre>   1. Go to a secure site (ex: <a href="https://www.wamu.com">https://www.wamu.com</a>)  2. On the location bar  hover the mouse over the lock icon    Expected:  A bubble show some info describing the site as safe.  Actual:  An empty bubble shows.    This is caused by a regression in label.cc SizeToFit() does not set the   width of the label. </pre>