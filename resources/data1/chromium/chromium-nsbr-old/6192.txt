<title>Issue 6192 -   chromium -    Window.open parameters for top and left are ignored - Project Hosting on Google Code</title>      function wopen(url  name  w  h)  {    // Fudge factors for window decoration space.    w += 32;    h += 96;    wleft = (screen.width - w) / 2;    wtop = (screen.height - h) / 2;    // IE5 and other old browsers might allow a window that is    // partially offscreen or wider than the screen. Fix that.    if (wleft &lt; 0) {      w = screen.width;      wleft = 0;    }    if (wtop &lt; 0) {      h = screen.height;      wtop = 0;    }    var win = window.open(url       name       'width=' + w + '  height=' + h + '  ' +      'left=' + wleft + '  top=' + wtop + '  ' +      'location=no  menubar=no  ' +      'status=yes  toolbar=no  scrollbars=yes  resizable=no');    // Just in case width and height are ignored    win.resizeTo(w  h);    // Just in case left and top are ignored    win.moveTo(wleft  wtop);    win.focus();  }        Should produce a new centered javascript window matching the dimensions   entered when the script is called.      Produces a collapsed window only a few cm sq. It does however appear to be   centered on the screen.    Please provide