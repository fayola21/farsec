<title>Issue 8900 -   chromium -    Some pages load cached version instead of current version on initial load - Project Hosting on Google Code</title>   1. Go to <a href="http://www.leasticoulddo.net/">http://www.leasticoulddo.net/</a> for the first time.   2. Come back the next day and load from most visited or type in URL and   autocomplete.  3. Old version loads (web comic updated daily)      Expect to see today's comic      I usually see the first cached version I load in this instance of the   browser.. not entirely sure if that's 100% true but it's always an older   cached version. I can do a hard refresh and the problem is normally   corrected for the current browsing session but somehow later loads still   pull up the old cached version.    <b>Please provide