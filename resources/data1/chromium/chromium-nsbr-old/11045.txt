<title>Issue 11045 -   chromium -    CRASH dragging multple files and dropping them on the browser content area - Project Hosting on Google Code</title> <pre> CRASH dragging multple files and dropping them on the browser content area    the crash looks like memory corruption.  the culprit is a missing sizeof(T)   in WebVector allocation routine :(    this may be a trunk only issue at the moment. </pre>