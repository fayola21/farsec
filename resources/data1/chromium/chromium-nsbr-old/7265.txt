<title>Issue 7265 -   chromium -    InitExtensions crashes during browser startup on OS X - Project Hosting on Google Code</title> <pre> in BrowserInit::LaunchWithProfile::Launch  the call to profile-&gt;InitExtensions is #ifdef'ed out   currently because the function crashes. Erik says it works in the unit test so it's possible that   something related to the chrome executable or the browser startup sequence differs from the unit   test in a way that causes this </pre>