<title>Issue 650 -   chromium -    Improper evaluation of regex is causing Chrome to bypass the form data validation - Project Hosting on Google Code</title>   1. If you click the Submit button (without filling out the form) - it   should output a validation error saying &quot;you need to enter a first name    (or whatever field is not filled out)&quot; and so on.  Instead  it skips the   validation and submits empty information to the database.  2. This works fine on all other browsers.  It's a fairly simple validation   script.  I've attached the javascript file and the php file it's associated   to.         A Validation error should pop up      It submits the empty fields.    Please provide