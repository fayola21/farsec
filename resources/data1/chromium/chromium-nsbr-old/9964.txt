<title>Issue 9964 -   chromium -    Lack command line parameter to specify the path of PAC script. - Project Hosting on Google Code</title> <pre> Chrome Version       : 2.0.173.1    Chrome now use V8 to evaluate PAC scripts by default. But it still rely on   IE's setting to determine the path of PAC file.    Chrome should have a command line parameter to specify the path of PAC   script.       </pre>