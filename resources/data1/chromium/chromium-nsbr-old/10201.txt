<title>Issue 10201 -   chromium -    4 Failing Layout Tests - Project Hosting on Google Code</title> <pre> The following layout tests/directories are failing  but are not marked as WONTFIX in test_expectations.txt:   Merge 40165:40297 -- regressions  New test. We should fix it  but it doesn't need to block the current release  New test. We should fix it  but it doesn't need to block the current release  New test. We should fix it  but it doesn't need to block the current release  Expectations for this test changed upstream. We should fix this test  but  it doesn't need to block the current release   DEFER WIN LINUX : LayoutTests/fast/repaint/dynamic-table-vertical-alignment-change.html = FAILDEFER WIN LINUX : LayoutTests/fast/repaint/inline-color-change.html = FAILDEFER : LayoutTests/http/tests/misc/SVGFont-delayed-load.html = FAILDEFER : LayoutTests/http/tests/uri/resolve-encoding-relative.html = FAIL  (Automatically generated from line 1788 to line 1799 of test_expectations.txt) </pre>