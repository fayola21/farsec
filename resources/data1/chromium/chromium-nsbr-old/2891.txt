<title>Issue 2891 -   chromium -    document.documentElement.scrollTop always returns zero and can't be set - Project Hosting on Google Code</title>   1. Create a block element of fixed size and scroll:auto.  2. Read or set that element's scrollTop to any value.  3. Scrolltop is always zero.        The scrollTop property should set to a nonzero value and allow script to   set the scroll position of the element.        The scrollTop property remains zero and scroll position does not change.    Please provide