<title>Issue 15978 -   chromium -    Reader plugin loads but doesn't render content until we resize the browser - Project Hosting on Google Code</title>   1. Simply open the PDF in the browser with that URL      The TAGS bus route PDF is displayed.      The pages don't appear  but the plugin loads. The borders of the plugin   (toolbars  etc.) are shown  but there is just a black area where the pages   should be. If I try to scroll down  the plugin freezes. Pushing the back   and forward navigation buttons can cause a browser freeze  which is   recoverable by pushing back again and waiting. Sometimes right clicking the   browser bar makes it unfreeze quicker.    <b>Please provide