<title>Issue 2434 -   chromium -    Regression: tooltips for tab only show up if more than one tab - Project Hosting on Google Code</title> <pre>   1. Create a window with only one tab.  2. Go to a site with a long tooltip.  3. Hover the mouse over the title so that the tooltip should appear. Notice   that it doesn't.  4. Create another tab.  5. Hover over the first tab  notice the tooltip appears.    This is with ToT. </pre>