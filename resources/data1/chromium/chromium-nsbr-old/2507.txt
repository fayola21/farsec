<title>Issue 2507 -   chromium -    Post or Delete xhr with no content sends &quot;Content-Type: application/xml&quot; header - Project Hosting on Google Code</title>   1. Create a page with javascript that uses xmlHttpRequest to do a POST or   DELETE with no content  2. Notice that Google Chrome sends the &quot;Content-Type: application/xml&quot;   request header  while other browsers don't send a &quot;Content-Type&quot; header at   all.     This is breaking my existing code since my API endpoints strictly accept   certain Content-Types. It is a simple fix (accept any content type for APIs   that expect blank content) but I figured I raise it anyway.    Thanks                Please provide