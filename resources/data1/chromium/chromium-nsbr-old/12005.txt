<title>Issue 12005 -   chromium -    Handle IPv6 literal addresses in URLs and proxy lists correctly - Project Hosting on Google Code</title> <pre> Our network stack should handle IPv6 literal addresses in URLs  proxy  lists  etc. correctly.  In particular  we need to include or strip the  square brackets used to separate an IPv6 literal address from a port.    <a title="Implement IP/CIDR matching for ProxyService::ShouldBypassProxyForURL()" class=closed_ref href="/p/chromium/issues/detail?id=9835"> Issue 9835 </a> can be considered a part of this issue. </pre>