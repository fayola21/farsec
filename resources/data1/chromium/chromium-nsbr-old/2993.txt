<title>Issue 2993 -   chromium -    Tab is dragged from window even after mouse is released - Project Hosting on Google Code</title> <pre> Product Version      : &lt;see about:version&gt;                  It's hard to tell exactly what the pattern is. Sometimes it happens    sometimes it doesn't. But if you click on a tab  then let go and move the   mouse  the tab will rip off the window as if you never let go of the mouse.   </pre>