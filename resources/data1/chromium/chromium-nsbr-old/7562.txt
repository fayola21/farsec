<title>Issue 7562 -   chromium -    Chrome Javascript console logs errors on test access to cross domain frames  - Project Hosting on Google Code</title>   1. setup a top frame that includes an iframe from a different domain  2. open javascript console  3. have javascript from top frame test the inclluded frame and catch errors     function isAcessibleWindow(w) {        try {          return w['foo'] == true;        } catch (e) {          return false;        }     }    4. Observe errors logged anyway (Unsafe JavaScript attempt to access frame   with URL .........)          Should not log errors if the exception is caught        Logs lots of errors    Please provide