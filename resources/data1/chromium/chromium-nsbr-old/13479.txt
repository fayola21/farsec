<title>Issue 13479 -   chromium -    Cookie and Set-Cookie headers don't show up in inspector - Project Hosting on Google Code</title>   1. Open inspector  2. Browse to a page that uses a Cookie  3. Note that the Set-Cookie or Cookie header is missing.      jamesr and I (zundel) looked into this a bit.  The problem seems to stem  from the fact that the instrumentation for sending headers over to  inspector is done in the renderer  but it is the browser that handles cookies.                <b>Please provide