<title>Issue 9081 -   chromium -    User can drag a single tab with single window on tabstrip - Project Hosting on Google Code</title> <pre> Build: 2.0.171.0 (Developer Build 12189)    -Have a single tab with single window opened.    User can drag the tab on tab strip [behind the + button]  can pull the tab  out of the tabstrip  as if the user can drag and drop the tab into a new  window.    Observation:  If user drags and drops to a new location  one more chromium button appears  on taskbar and quickly disappears.     </pre>