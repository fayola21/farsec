<title>Issue 21383 -   chromium -    Make the data writes of the http cache asynchronous - Project Hosting on Google Code</title> <pre> Currently the http cache performs synchronous data writes and asynchronous   data reads. We should convert them to asynchronous.    note: usually this is not noticeable... unless the system is under heavy load   or the disks are behaving poorly. </pre>