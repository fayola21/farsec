<title>Issue 22631 -   chromium -    NPObject identity is not preserved when passed from the plugin to the browser - Project Hosting on Google Code</title>   1. Have a plugin that implements npruntime scripting interface  2. Implement a function on the plugin that returns the same NPObject  always  say getNPObject.  3. Get an instance of that plugin in JavaScript  4. Call twice the function and check for object identity    obj1 = plugin.getNPObject();  obj2 = plugin.getNPObject();  alert(obj1 === obj2);  alert(obj1 == obj2);        obj1 === obj2 &amp;&amp; obj1 == obj2 should return true        obj1 === obj2 &amp;&amp; obj1 == obj2 returns false    <b>Please provide