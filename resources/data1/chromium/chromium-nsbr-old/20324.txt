<title>Issue 20324 -   chromium -    Layout test crash from 47732:47748 - Project Hosting on Google Code</title> <pre> WIN LINUX : LayoutTests/http/tests/security/javascriptURL/javascriptURL-in-  new-iframe.html = CRASH    The first crash happens when calling updateDocumentWrapperCache. This   because m_context can not be initialized when the frame is shutting down.    void V8Proxy::updateDocument()  {      ...      initContextIfNeeded();        // We have a new document and we need to update the cache.      updateDocumentWrapperCache();        updateSecurityOrigin();  }    The second crash happens because webview is NULL.    void WebFrameLoaderClient::dispatchDidFinishDocumentLoad() {    WebViewImpl* webview = webframe_-&gt;GetWebViewImpl();    WebViewDelegate* d = webview-&gt;delegate();    ...  }   </pre>