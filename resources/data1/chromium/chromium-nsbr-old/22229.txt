<title>Issue 22229 -   chromium -    Chrome Dev - Gmail loading issues - Project Hosting on Google Code</title>   1. enter login  pass  2. log into gmail  3. wait for it to load and it never does properly (chat missing or messages   never load)  4. Stare at messages trying to open but it never happens        Gmail loads up in Google Chrome just like in another other browser (i.e.   Firefox)        Gmail fails to load and prompts the user to load in simple html format   instead.    <b>Please provide