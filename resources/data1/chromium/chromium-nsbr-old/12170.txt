<title>Issue 12170 -   chromium -    Last user script not unloaded properly - Project Hosting on Google Code</title>   1. Copy a single userscript.user.js file to the User Scripts directory  2. Load a web page that triggers the user script  3. Remove the user script so the User Scripts directory is empty  4. Reload the page (user script still runs)      After the last user script is unloaded  it should not run any more.      Last user script does not get unloaded from current pages. One must  go to a new page to have the unload take effect.    <b>Please provide