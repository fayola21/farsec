<title>Issue 3995 -   chromium -    Auto Scroll (middle mouse button) too fast - Project Hosting on Google Code</title>   1. Click the middle mouse button  2. Move the mouse just a tiny tiny bit below where you clicked  3. The auto scroll is a little too fast. It should be as slow as Firefox   can go when the mouse is just moved a tiny tiny bit from where you clicked   the middle mouse button.      Try the autoscroll in Firefox  it can go slower than Chrome's if moved just   a tiny tiny bit from where you clicked.      The auto scroll is a little too fast. It should be as slow as Firefox can   go when the mouse is just moved a tiny tiny bit from where you clicked the   middle mouse button.    Please provide