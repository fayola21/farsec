<title>Issue 17796 -   chromium -    Content scripts load for non-html (.txt  .jpg) documents - Project Hosting on Google Code</title> <pre> Chrome Version       : 3.0.195.1 (Official Build 21222)      1. Open chrome with an extension with a content script  2. Load a non-HTML file which matches the content script (e.g. .txt  .jpg)      The content script does not execute.      The content script executes.    Example extension attached (unzip  then visit the .txt file  and check the  console.log)    Seems to happen over http:// https:// and file:// - have not tested ftp. </pre>