<title>Issue 6120 -   chromium -    RTL: Find overlaps the Chrome window border if not enough space to move - Project Hosting on Google Code</title> <pre> Open the Find box in an RTL language (such as Hebrew)  Resize the window horizontally to make it very small.    Notice that once the window is smaller than the width of the Find box the  Find box will overlap the window border.   </pre>