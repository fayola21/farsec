<title>Issue 22703 -   chromium -    All subprocess creation needs to go through zygote - Project Hosting on Google Code</title> <pre> For stability across updates  we need all subprocess creation to go through   the zygote.     - utility process   - renderer process (done)   - plugin process   - worker process   - other process types in the future    I think the fix involves moving ZygoteMain() up above the list of tests   against process_type  so that any process type can be spawned via the   zygote.  Then fixing all the call sites that spawn helper processes to use   a general function for zygoting rather than copying code around (which is   what we've done so far :(). </pre>