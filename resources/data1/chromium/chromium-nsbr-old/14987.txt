<title>Issue 14987 -   chromium -    On chrome startup with restore pages that were last open option is set  all the tabs from previous session are loaded into separate renderer processes. - Project Hosting on Google Code</title>   1. Start Chrome  open multiple websites in separate tabs and then open   links from each of the websites again in separate tabs.  2. Close the main browser window to close all the tabs.  3. Start Chrome again.      In 1. above we can see that renderer processes are grouped together for   each website/domain.  In 3. we should see that the processes are rendered similarly.      In 3. we observe that all the tabs are rendered using separate processes.    <b>Please provide