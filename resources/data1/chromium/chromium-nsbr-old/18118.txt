<title>Issue 18118 -   chromium -    Render view context menu should show (more) accelerators - Project Hosting on Google Code</title> <pre> currently windows fakes some common accelerators for editing text:    bool RenderViewContextMenuWin::GetAcceleratorForCommandId(      int command_id       views::Accelerator* accel) {    // There are no formally defined accelerators we can query so we assume    // that Ctrl+C  Ctrl+V  Ctrl+X  Ctrl-A  etc do what they normally do.    switch (command_id) {      case IDS_CONTENT_CONTEXT_UNDO:        *accel = views::Accelerator(L'Z'  false  true  false);        return true;        case IDS_CONTENT_CONTEXT_REDO:        *accel = views::Accelerator(L'Z'  true  true  false);        return true;        case IDS_CONTENT_CONTEXT_CUT:        *accel = views::Accelerator(L'X'  false  true  false);        return true;        case IDS_CONTENT_CONTEXT_COPY:        *accel = views::Accelerator(L'C'  false  true  false);        return true;        case IDS_CONTENT_CONTEXT_PASTE:        *accel = views::Accelerator(L'V'  false  true  false);        return true;        case IDS_CONTENT_CONTEXT_SELECTALL:        *accel = views::Accelerator(L'A'  false  true  false);        return true;        default:        return false;    }  }    but aside from that  there are no shortcuts displayed in the menu (e.g.   print  save as  back  forward  reload). I'm not sure how much plumbing   would be required. Hopefully not a massive amount. For Linux  we will have   an even harder time  as editing commands can change based on the keyboard   theme (assuming that functionality ever gets added). </pre>