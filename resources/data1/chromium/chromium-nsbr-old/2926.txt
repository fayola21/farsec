<title>Issue 2926 -   chromium -    When bookmark a page without charset in http header and meta charset  Chrome should record what Encoding the page uses. - Project Hosting on Google Code</title> <pre> See &lt;b1091216&gt;    When bookmark a page without charset in http header and meta charset   Chrome should record what Encoding the page uses.    For example  open a page without http header or meat charset  use auto  detect to get the right page  bookmark it and then turn off auto detect   clear the cache  next time when we open the page from bookmark  we still  get a garbage page.    FF supports this feature  but IE7 doesn't.    </pre>