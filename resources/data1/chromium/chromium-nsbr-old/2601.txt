<title>Issue 2601 -   chromium -    target=_blank link breaks the process-per-tab mode - Project Hosting on Google Code</title>   1. launch Chromium with the --process-per-tab option  2. launch the Task Manager  3. click on a link with target=_blank attribute (for example  click any   link in the above w3c page)  4. navigate to other sites in the newly opened tab         the newly opened tab should have its own process      the newly opened tab shares the same process with the originating tab  even   after you navigate to other sites in it    Please provide