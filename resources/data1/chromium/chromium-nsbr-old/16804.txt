<title>Issue 16804 -   chromium -    Pressing the 'stop' button doesn't stop loading the page - Project Hosting on Google Code</title>   1. On a moment that your internet-connection-speed is very slow go to a   large page  2. Before it finished loading press the &quot;X&quot; button (it is on the right end   of the address bar. (If you aren't loading a page it displays a 'play'   icon).  3. Notice that it doesn't stop loading the page before the whole page is   loaded. This button has no effect thus      To immediately stop loading the page      It continues    <b>Please provide