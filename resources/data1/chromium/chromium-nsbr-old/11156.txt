<title>Issue 11156 -   chromium -    Find in Find Bar not actually finding all the matching entries. - Project Hosting on Google Code</title>   1. Open the web page url provided above  2. Press CTRL ^ F to open Find Bar and type code_signing  3. Press Next option in Find Bar to go to next matching string  4. You will find that at some times  it won't highlight the matching   string. But  simply it make the matching string visible by scrolling the   page accordingly  5. If you type ode_signing instead of code_signing  then it will highlight   for all matching entries as expected.      It should highlight all the matching entries  when we type code_signing   also.      It does not highlighting all the matching entries  when we type code_signing and search for using Find Bar.    <b>Please provide