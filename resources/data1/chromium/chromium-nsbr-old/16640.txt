<title>Issue 16640 -   chromium -    DataSource does asynchronous reads - Project Hosting on Google Code</title> <pre> We need to isolate the blocking read semantic of FFmpeg and put it only into   FFmpegDemuxer. </pre>