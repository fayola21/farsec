<title>Issue 16879 -   chromium -    Custom background tab image not aligned to top of tab - Project Hosting on Google Code</title> <pre> If a theme provides a custom background tab image  we currently align that   image to the top of the frame rather than the top of the tab. This is   wronggetty wrong. </pre>