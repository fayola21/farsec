<title>Issue 17180 -   chromium -    When canceling a download in the downloadsbar the number of bytes downloaded is displayed without ' bytes' after it - Project Hosting on Google Code</title>   1. Start a download  2. In the downloadbar on the bottom of the screen select arrow --&gt; cancel  3. The number of bytes downloaded will be displayed  but no ' bytes' is   placed after it. It is also not rounded to an other unit (like MB or GB)      To see bytes after it  or (what I prefer even more) not to display the   amount downloaded. Why would you like to know the number of bytes   downloaded? You can't even use the file because a canceled download is   automatically deleted      You see the number of bytes  followed by &quot;  cancelled&quot;    <b>Please provide