<title>Issue 17480 -   chromium -    Implement a separate thread to contain all FFmpeg demuxer method calls in FFmpegDemuxer - Project Hosting on Google Code</title> <pre> We don't want FFmpeg demuxer method calls to block the demuxer thread  so we   need to move them to a separate thread. </pre>