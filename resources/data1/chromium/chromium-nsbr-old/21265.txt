<title>Issue 21265 -   chromium -    On Vista  you can get into a state where there is no default printer even if multiple printers are installed - Project Hosting on Google Code</title> <pre>   1. Make sure multiple printers are installed and make one the default  2. Uninstall the default printer  3. Try to print in chromium      Chromium fails to print since it can't find the default printer default   settings.   </pre>