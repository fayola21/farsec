<title>Issue 8918 -   chromium -    Inspector erroneously shows and renders property &quot;overflow:hidden&quot; which hides entire table - Project Hosting on Google Code</title>   I had submitted a bug through chrome's built-in reporter  thought I should mention it in the correct area.  We use an internal   ticketing system that has worked on chrome until 2.0.169.1.  Now tickets (large tables) show up as blank black pages.  The   Inspector shows the property &quot;overflow:hidden&quot; simply unchecking this property makes the page appear like normal.  I checked   the source and this property is set to &quot;overflow: auto&quot;      Page should be rendered normally      Page is rendered entirely hidden  although it is scrollable and Ctrl-A  Ctrl-C copies the hidden text    <b>Please provide