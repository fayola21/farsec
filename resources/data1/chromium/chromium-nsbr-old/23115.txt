<title>Issue 23115 -   chromium -    Add regression tests for whiteout duration on startup  tab creation - Project Hosting on Google Code</title> <pre> Once we have a &quot;loaded machine&quot; simulator  we should create whiteout duration   tests so we can detect regressions.    This was originally listed as part of <a title="add consume to a step in the tab-switching tests"  href="/p/chromium/issues/detail?id=21929">bug 21929</a>  but I'm breaking it out   here. </pre>