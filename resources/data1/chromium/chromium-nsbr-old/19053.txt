<title>Issue 19053 -   chromium -    Middle click sends Chromium to background - Project Hosting on Google Code</title>   1. Make sure there is another window open behind Chromium.  2. Click the middle mouse button (ie.  the scroll wheel) on a link or the   page        If on a link  open link in new tab; if on a blank/empty area  start   autoscrolling; if in a text area  paste text; if on scrollbar  jump to that   location.        Acts as expected (except for autoscrolling)  but sends window to   background at the same time.    <b>Please provide