<title>Issue 23077 -   chromium -    Background color issue on certain pages - Project Hosting on Google Code</title>   1. Load web page with background color set  2. if the page height is smaller than the browser window it will not fill   the background color to the bottom of the browser window  3. focus (click) another window/aplication and focus back the browser   window  now the background color is on the whole page from top to bottom.      Fill background color on whole page      Fills background color only on body size      <b>Please provide