<title>Issue 7314 -   chromium -    Add 4 fallback mappings to GBK converter in ICU - Project Hosting on Google Code</title> <pre> Webkit adds 4 fallback mappings to ICU's GBK converter in   WebCore/platform/text/TextCodecICU.cpp when converting from Unicode.     We also use ICU converters outside the webkit. For instance  we use it for   form submission(get) and url encoding (query part). We'd better make both   usage behave identically. This can be done by modifying the GBK conversion   table in our copy of ICU.       </pre>