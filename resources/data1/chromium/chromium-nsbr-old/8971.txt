<title>Issue 8971 -   chromium -    Shell env not passed to pkg-config - Project Hosting on Google Code</title> <pre> Chrome Version       : repo version 12076       initiate build with ../third_party/scons/scons.py [...]       scons invokes pkg-config --cflags --libs nss   and finds the desired package(s)       Cannot find the requested package(s) because they are installed in    /usr/local/.   pkg-config allows setting PKG_CONFIG_PATH  but the env variable is not   passed from the shell through scons into the pkg-config invocation.   </pre>