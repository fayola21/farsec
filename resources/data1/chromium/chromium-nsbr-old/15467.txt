<title>Issue 15467 -   chromium -    Crash on new tab page with not tips - Project Hosting on Google Code</title> <pre>   1. Clear your cached tips.  2. Go to the new tab page  3. Click on the placeholder link &quot;Tips and recommendations to help you   discover interesting websites.&quot;        Crashes due to an assert in new_tab_ui.cc:    void NewTabHTMLSource::StartDataRequest(const std::string&amp; path                                           int request_id) {    if (!path.empty()) {      // A path under new-tab was requested; it's likely a bad relative      // URL from the new tab page  but in any case it's an error.      NOTREACHED();      return;    }      Miranda  you need to make this into a non link. </pre>