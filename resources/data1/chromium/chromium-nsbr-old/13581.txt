<title>Issue 13581 -   chromium -    When selecting another (old) tab  it drags it automatically into another window - Project Hosting on Google Code</title>   1. Not using a tab for a long time so that Chrome has to rebuild the page   when clicking on the tab  2. Selecting the tab  3. Moving the mouse a bit while Chrome is still rebuilding the page (can   take up to 3 seconds)      # After rebuilding the tab  it should just be displayed as normal      # The tab is automatically being dragged into another window  not staying   in the main Chrome window    <b>Please provide