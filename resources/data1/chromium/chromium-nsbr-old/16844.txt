<title>Issue 16844 -   chromium -    Gmail + Flash = unresponsive plugin process + 100% CPU - Project Hosting on Google Code</title>   0. install libflashplugin.so into the plugins/ directory  launch chrome --  enable-plugins  1. open <a href="https://mail.google.com/">https://mail.google.com/</a>  2. log in  wait for all the tabs &amp; gadgets to load  3. observe in top that a chrome process takes most of the CPU with X taking   the rest  4. open a new tab at <a href="http://www.youtube.com/">http://www.youtube.com/</a>  5. observe that the page doesn't load      Chrome should not take 100% of the cpu.  This should not prevent other pages that need flash from running.      See steps 3 and 5 above.    <b>Please provide