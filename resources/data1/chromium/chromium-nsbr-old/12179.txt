<title>Issue 12179 -   chromium -    System font hinting settings are not obeyed - Project Hosting on Google Code</title> <pre> Chrome Version: 2.0.181.0~svn20090517r16261-0ubuntu1~ucd1~jaunty  OS + version: Ubuntu 9.04  Window manager: GNOME/Metacity  URLs (if applicable): -  Behavior in Firefox 3.x (if applicable): font settings from GConf are  obeyed when using the default package  and those from ~/.fonts.conf when  using a build from Mozilla.      1. In GNOME  open gnome-appearance-properties.  2. Go to the Fonts tab and open Details. Disable font hinting.    These steps disable font hinting in GConf. Also  I tried modifying  ~/.fonts.conf:    &lt;?xml version=&quot;1.0&quot;?&gt;  &lt;!DOCTYPE fontconfig SYSTEM &quot;fonts.dtd&quot;&gt;  &lt;fontconfig&gt;    &lt;match target=&quot;font&quot;&gt;      &lt;edit name=&quot;hinting&quot; mode=&quot;assign&quot;&gt;&lt;bool&gt;false&lt;/bool&gt;&lt;/edit&gt;    &lt;/match&gt;  &lt;/fontconfig&gt;      Chromium should apply the font settings stored in GConf  and maybe fall  back to those in ~/.fonts.conf.      Only the chrome (tab bar  menus  ...) uses the correct font settings. The  text in the Web content keeps having hinted fonts. </pre>