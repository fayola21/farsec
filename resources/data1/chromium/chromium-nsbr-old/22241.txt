<title>Issue 22241 -   chromium -    Passwords saved plaintext and world-readable - Project Hosting on Google Code</title>   1. Enable password saving (enabled by default)  2. Login to a site (e.g.  Gmail)  allowing Chrome to save the password  3. Have a look at the permissions on &quot;~/Default/Web Data&quot; and its content        Passwords should be encrypted  ideally with strong encryption  or at least   with weak obfuscation  and the filesystem-level permissions on the sqlite   database containing them should be 0600 (i.e.  read/write by user only    with no group or world permissions set).        Passwords are plaintext inside the sqlite database  which has read   permissions for group and world (i.e.  anybody on the system can read the   passwords).    <b>Please provide