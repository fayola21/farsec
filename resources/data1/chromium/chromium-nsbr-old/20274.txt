<title>Issue 20274 -   chromium -    Videos without durations only play a single frame - Project Hosting on Google Code</title> <pre>   1. Play a video format where FFmpeg won't return the duration (i.e.  flv)      Video should play.  Instead we get the first frame.    The issue is PipelineImpl::GetCurrentTime() will not return a time greater   than the duration.  If the duration remains zero  then GetCurrentTime() will   always return zero. </pre>