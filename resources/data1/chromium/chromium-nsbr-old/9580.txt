<title>Issue 9580 -   chromium -    Inline SVG stroke rendering affected by CSS dotted border style - Project Hosting on Google Code</title>   1. Create an inline SVG polyline element with no dash array  2. Elsewhere on the page  create an HTML element with a dotted border style      The HTML element should show with a dotted border and the polyline SVG   element should show as a solid line.      Both the HTML element and SVG polyline show up as dotted.    <b>Please provide