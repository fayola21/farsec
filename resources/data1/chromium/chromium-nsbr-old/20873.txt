<title>Issue 20873 -   chromium -    Dragging tab always pops into new window; multiple monitors  Linux - Project Hosting on Google Code</title>   1. Open a new Chrome instance; call this window A  2. Create a new window; call this window B (I use the CTRL+N shortcut)  3. Drag window B to the second montor  3. Create a new tab in window B  4. Drag the newly created tab from window B into a new window (C) on the  same monitor  5. Create a new tab in window C  6. Try to drag that tab; the tab immediately pops into a new window without  letting you drag it around.    <b>Please provide