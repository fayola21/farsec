<title>Issue 20251 -   chromium -    Support for load event for audio/video over HTTP - Project Hosting on Google Code</title> <pre> Currently we're never finished loading so we never for load.    When we have full support for caching we can fire the load event. </pre>