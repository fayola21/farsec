<title>Issue 16638 -   chromium -    Move FFmpeg DLLs over to /deps and pull them in on a per-platform basis - Project Hosting on Google Code</title> <pre> Instead of having them all checked into /src/third_party/ffmpeg/binaries they   should instead be checked into /deps/third_party/ffmpeg/binaries and pulled   in via DEPS file. </pre>