<title>Issue 17487 -   chromium -    add virtual destructors to abstract interface classes in webkit api - Project Hosting on Google Code</title> <pre> From a code review:    &quot;To be safe  I suppose we should add  similar virtual destructors to other interfaces in the WebKit API just  in case the usage pattern of the embedder would benefit from it (as in  this case).&quot;   </pre>