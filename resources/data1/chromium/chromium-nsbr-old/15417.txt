<title>Issue 15417 -   chromium -    test fallback order does not match upstream webkit - Project Hosting on Google Code</title> <pre> There's been a number of cases recently where expected results have been   missing for new tests. This should never happen unless the person who   committed the test forgot to commit the results for the test. Given that the   upstream bots are green  I don't think that's the case here.    This implies to me that we have a bug in run_webkit_tests somewhere. We   should fix it. :)    If we find that there is a good and frequent reason for this to happen  then   we should consider adding a NEEDS_EXPECTATIONS modifier for those   cases rather than skipping them.    Marking as a P1 since this seems to have come up a lot recently and thus is   using up an inordinate amount of engineering time. </pre>