<title>Issue 18034 -   chromium -    Implement Clipboard::WriteBookmark - Project Hosting on Google Code</title> <pre> When you copy a URL from content  it calls a set of methods on the   clipboard:      scw.WriteBookmark(title  url.spec());    scw.WriteHTML(UTF8ToUTF16(URLToMarkup(url  title))  &quot;&quot;);    scw.WriteText(UTF8ToUTF16(url.spec()));    Right now we can paste URLs because of the WriteText.  Not sure what using gtk_selection_data_set_uris buys us. </pre>