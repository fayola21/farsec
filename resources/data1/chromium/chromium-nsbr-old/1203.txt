<title>Issue 1203 -   chromium -    Minimize button supersedes the &quot;X&quot; on a right-most tab in Vista - Project Hosting on Google Code</title>   1. Open a ton of tabs (so that there is one over the min/max/close boxes)  2. Hover over the &quot;X&quot; on the right-most tab.    3. Instead of the &quot;X&quot; getting a red circle like other tabs do  the minimize   button highlights instead.        Tab should close (or tab should not cover the min/max buttons)        Browser minimizes    Please provide