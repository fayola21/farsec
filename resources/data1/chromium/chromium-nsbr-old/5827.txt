<title>Issue 5827 -   chromium -    Implement ImageSource::filenameExtension - Project Hosting on Google Code</title> <pre> We're failing a couple of tests because of the addition of an ASSERT to   DragController.cpp  specifically:    ASSERT(!image-&gt;filenameExtension().isEmpty());    Our ImageSource returns an empty string from filenameExtension  and we crash. Adam landed   a patch upstream (<a href="/p/chromium/source/detail?r=39396">revision 39396</a>) that adds a filenameExtension method to ImageDecoder.   Once we get this version of WebKit we need to change ImageSource::filenameExtension to:    if (!m_decoder)    return String();   return m_decoder-&gt;filenameExtension();    And hopefully the following layout tests will then pass again    LayoutTests/editing/pasteboard/4947130.html  LayoutTests/editing/pasteboard/drag-image-in-about-blank-frame.html  LayoutTests/editing/pasteboard/drag-image-to-contenteditable-in-iframe.html  LayoutTests/editing/pasteboard/drag-selected-image-to-contenteditable.html  LayoutTests/editing/selection/drag-to-contenteditable-iframe.html  LayoutTests/fast/events/standalone-image-drag-to-editable.html </pre>