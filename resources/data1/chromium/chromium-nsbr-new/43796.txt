<title>Issue 43796 -   chromium -    setTimeout callbacks assigned in a mousemove event handler unexpectedly fire after a print dialog is opened - Project Hosting on Google Code</title>   1. Open the attached demo page  2. Open the print dialog and cancel it  3. The idle timer which was scheduled to run after an hour runs immediately        Each time a mouse move event fires  a setTimeout callback is set or reset   to fire the timeoutExpired function after 60 minutes.    When the user opens and then dismisses the print dialog (either by pressing   Print or Cancel) the timeoutExpired callback should not automatically fire.        The callback assigned in setTimeout fires ahead of schedule after opening   and dismissing the print dialog.    <b>Please provide