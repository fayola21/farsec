<title>Issue 24810 -   chromium -    CTRL+click on a URL on the console does not open the desired URL on the browser - Project Hosting on Google Code</title>   1. an URL is outputted on the console  2. hovering over it with the mouse get it underlined  3. press CTRL and click on the link      A new tab is added to the current window (a new one is opened if no chrome   windows are opened) and the URL is loaded in that tab / window.      A new window is opened (even if there is already an opened one) and the URL   is NOT loaded.    <b>Please provide