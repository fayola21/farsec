<title>Issue 36451 -   chromium -    Add geolocation null support to test_shell - Project Hosting on Google Code</title> <pre> test_shell currently relies on geolocation being disabled at runtime.  This is blocking webkit bug <a href="https://bugs.webkit.org/show_bug.cgi?id=35095">https://bugs.webkit.org/show_bug.cgi?id=35095</a>    Short term fix is to call       WebKit::WebRuntimeFeatures::enableGeolocation(false);  in test shell.    Full fix would be to call       WebKit::WebRuntimeFeatures::enableGeolocation(true); but have a dummy   geolocation service supplied that allows fast/dom/Window/window-  properties.html to pass.   </pre>