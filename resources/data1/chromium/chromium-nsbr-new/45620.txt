<title>Issue 45620 -   chromium -    onclick event fires extremely slowly  regression from older versions - Project Hosting on Google Code</title>   1. Create a html page with some CSS and a table with 4000 rows  each with a   div that has a bunch of css classes and has an onclick handler which shows   an alert. See attached file for an example.  2. Click the row      The onclick handler is immediately invoked  resulting in the alert showing   instantly      There is a long  noticeable lag (~1.5 seconds for me) after the click and   before the alert appears. Repro on Mac and PC (same build of Chrome).  This is likely a regression - previous versions of Chrome didn't exhibit   this latency.    <b>Please provide