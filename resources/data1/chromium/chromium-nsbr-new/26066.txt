<title>Issue 26066 -   chromium -    We need a unit test to verify that window.open calls issued within ChromeFrame make it back to the host - Project Hosting on Google Code</title> <pre> We need a unit test which verifies the following:-  1. Launches the host browser (e.g. IE) and navigates to a page which      transitions to ChromeFrame.  2. The page issues a window.open call to open another page which should send     a notification back to the host about the open call.     </pre>