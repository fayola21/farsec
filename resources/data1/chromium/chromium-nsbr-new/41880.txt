<title>Issue 41880 -   chromium -    If-Modified-Since header is not sent when fetching HTML files - Project Hosting on Google Code</title>   1. Load a URL to a static HTML file  2. Load that same URL again  3. If-Modified-Since header is not sent  so server is unable to produce a   304-Not Modified response      If-Modified-Since header should be sent  so server can return a proper 304   or 200 response when appropriate  to allow the browser to cache the page.        The header is NOT sent  so html pages are never cached (afaik)      <b>Please provide