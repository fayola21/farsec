<title>Issue 27185 -   chromium -    Symlinks within an extension directory will fail to load - Project Hosting on Google Code</title> <pre>   1. Create an extension directory with contains a symlink to a resource   elsewhere in the file system  2. Load the extension via --load-extension  3. The extension will fail to load complaining about the symlink files    I believe this worked prior to this patch:     <a href="http://codereview.chromium.org/343003">http://codereview.chromium.org/343003</a>    The file_util::PathExists() returns false (verified on windows only) if the   file is a symlink.       </pre>