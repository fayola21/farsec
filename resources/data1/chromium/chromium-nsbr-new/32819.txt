<title>Issue 32819 -   chromium -    notifications don't get enabled properly when connection is lost and then restored - Project Hosting on Google Code</title> <pre> Spawning off from 30156      1. Open Chromium (and login to Sync)  2. Open about:sync (Notifications should be enabled)  3. Take down the network connection  4. Wait until about:sync shows notifications are disabled  5. Bring up the network connection        about:sync should show notifications are enabled within a minute or so.  It   doesn't on non-Windows platforms. </pre>