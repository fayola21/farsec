<title>Issue 25939 -   chromium -    ParamTraits for UploadData truncates the int64 identifier down to int - Project Hosting on Google Code</title> <pre> ParamTraits for UploadData truncates the int64 identifier down to int    This means that we might end up with some unintended cache collisions.  But    those are probably rare given the way the identifiers are generated   (incrementing a counter initialized w/ the browser startup time). </pre>