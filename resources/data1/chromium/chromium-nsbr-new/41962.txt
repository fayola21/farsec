<title>Issue 41962 -   chromium -    (rich text) paste on linux adds extra character(s) at the end of the string - Project Hosting on Google Code</title>   1. select some text (including URL from the omnibox  that's how I found it)  2. paste in pidgin      text (or URL) should appear as copied      page text comes up with a bunch of text after it  then on subsequent pastes   it stabilises  either with a constant text after it  or the copied text as   expected  Omnibox links are a totally different beast: for every paste action  either   nothing appears  or the link does come up  but with a random extra   character appended    When selecting &quot;paste as plain text&quot;  results are what's expected (copied   text appears  nothing extra)    <b>Please provide