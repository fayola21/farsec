<title>Issue 26507 -   chromium -    Missing malloc check in base/linux_util.cc :: BGRAToRGBA() - Project Hosting on Google Code</title> <pre> Chrome Version  Rev: 30697          <a href="http://src.chromium.org/viewvc/chrome/trunk/src/base/linux_util.cc?annotate=30433#l157">http://src.chromium.org/viewvc/chrome/trunk/src/base/linux_util.cc?annotate=30433#l157</a>              Missing NULL check for malloc() (If it's a glibc malloc() not a wrapper).    ..    uint8_t* new_pixels = static_cast&lt;uint8_t*&gt;(malloc(height * stride));      // We have to copy the pixels and swap from BGRA to RGBA.    for (int i = 0; i &lt; height; ++i) {      for (int j = 0; j &lt; width; ++j) {        int idx = i * stride + j * 4;        new_pixels[idx] = pixels[idx + 2];        new_pixels[idx + 1] = pixels[idx + 1];        new_pixels[idx + 2] = pixels[idx];        new_pixels[idx + 3] = pixels[idx + 3];      }    }  ... </pre>