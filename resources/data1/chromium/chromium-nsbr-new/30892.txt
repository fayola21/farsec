<title>Issue 30892 -   chromium -    FTP url's should open in default app on Mac OS X - Project Hosting on Google Code</title>   1. visit a page with an <a href="ftp://example.com/">ftp://example.com/</a> url in an &lt;a href=&quot;&quot;&gt; tag  2. click the link        URL is opened in your default app for ftp:// url's as per the Mac OS X preferences setting.        Chrome does a directory listing of the FTP server.    <b>Please provide