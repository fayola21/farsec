<title>Issue 26312 -   chromium -    Scripts panel is empty on navigation to a different domain - Project Hosting on Google Code</title> <pre>   1. Open NTP.  2. Open DevTools using Ctrl+Shift+J(it important to not use 'Inspect   Element' action) and make sure Elements panel is selected by default. If   it's not close DevTools and repeat steps 1 2.  3. Navigate to google.com  4. Open Script panel.    What is the expected output?  Scripts menulist is populated with the page's scripts.    What do you see instead?  The script list is empty.   </pre>