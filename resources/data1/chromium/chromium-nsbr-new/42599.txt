<title>Issue 42599 -   chromium -    sessionStorage doesn't work in extensions - Project Hosting on Google Code</title> <pre> Try to following code in an extension popup:    -----  &lt;h3 id=&quot;foo&quot;&gt;No value&lt;/h3&gt;  &lt;script&gt;  window.onload = function() {    if (!sessionStorage.foo) {      sessionStorage.foo = 1;    } else {      sessionStorage.foo = parseInt(sessionStorage.foo) + 1;    }        document.getElementById(&quot;foo&quot;).innerHTML = &quot;&quot; + sessionStorage.foo;  };  &lt;/script&gt;  -----    Repeated opening of the popup will just continue to show 1. If you open the same resource as   a tab contents (chrome-extension://&lt;id&gt;/...)  Every refresh will increment. </pre>