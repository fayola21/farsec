<title>Issue 42642 -   chromium -    Chrome tests use a different directory structure for user data from what chromium uses. - Project Hosting on Google Code</title>   1. Run a chrome test (any integration test will do) with a particular  user_data_dir  2. Run an instance of chromium (where user_data_dir is under  %APPDATA%localChromium    What is the expected result? Both 1 and 2 use the same internal directory  and file structure under user_data_dir    What happens instead? The directory structures are different.    <b>Please provide