<title>Issue 43274 -   chromium -    Restoring a session should restore window minimization state - Project Hosting on Google Code</title> <pre>   1. Open some windows  2. Minimize one or more  3. Quit  with the &quot;On startup: Reopen the pages that were open lest&quot; pref selected  4. Relaunch    We remember window position  but not minimization state (which is a form of positioning). Camino   gets this right  so we should too ;) </pre>