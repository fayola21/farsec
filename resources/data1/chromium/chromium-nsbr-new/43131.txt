<title>Issue 43131 -   chromium -    re-enabling disabled native view does not - Project Hosting on Google Code</title> <pre> This is a bug in linux_views/chromeos build.      TextField* text_field = new TextField();    move focus to the text_field and disable it by      text_field-&gt;SetEnabled(false);    and then re-enabling the text field later      text_field-&gt;SetEnabled(true);    does not return the focus to the text field. </pre>