<title>Issue 41872 -   chromium -    hidding http breaks copy&amp;pasteability of URLs - Project Hosting on Google Code</title> <pre> Chrome Version       : 5.0.375.3 (Build oficial 44229) dev  URLs (if applicable) : any not starting with www      Firefox 3.x: OK      1. open any site which doesn't start with www  2. copy the URL into any application that aims to detect URLs (like   openoffice  gmail  IM client  whatever...)  3. oops  not a clickable        the URL prefixed with the scheme (http:// so when pasted the other   application knows it's a URL          the pasted URL without www in the server name is taken by every application   as raw text   </pre>