<title>Issue 36805 -   chromium -    TooltipManagerWin uses protected member variables in contravention of C++ style guide - Project Hosting on Google Code</title> <pre> Style guide says &quot;Make all data members private  and provide access to them through accessor functions as   needed&quot;  yet TooltipManagerWin has the following protected data members:      // Tooltip control window.    HWND tooltip_hwnd_;      // Tooltip information.    TOOLINFO toolinfo_;      // Last location of the mouse. This is in the coordinates of the rootview.    int last_mouse_x_;    int last_mouse_y_;      // Whether or not the tooltip is showing.    bool tooltip_showing_;    Noticed during <a href="http://codereview.chromium.org/652104/show">http://codereview.chromium.org/652104/show</a>   </pre>