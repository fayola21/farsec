<title>Issue 25040 -   chromium -    proxy settings not honored - Project Hosting on Google Code</title>   1. Configure an automatic proxy via Options which refers to a URL on an   internal network which is sometimes connected and sometimes not  e.g.   <a href="http://wpad.corp.google.com/wpad.dat">http://wpad.corp.google.com/wpad.dat</a>    2. Visit a web page that should only be visible on the corporate network    3. Note that Chrome shows a 404 even though the page is available  and the   proxy file is reachable.    4. Note that reloading does not help    5. Restarting all chrome processes fixes the issue temporarily      Internal network page should be visible      Chrome refuses to use configured proxy    <b>Please provide