<title>Issue 25636 -   chromium -    Popup can't open a new window and close itself - Project Hosting on Google Code</title>   1. Open a popup using window.open();  2. In the popup  an anchor should have an onclick function which calls window.open() and right   after window.close() to close itself;        A new window should be opened and the opener (the popup) should be closed.      A new window is opened but the opener (the popup) is not closed.    <b>Please provide