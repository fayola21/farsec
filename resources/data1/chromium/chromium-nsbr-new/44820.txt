<title>Issue 44820 -   chromium -    Chrome crashed when dragging a image composed by data URI - Project Hosting on Google Code</title> <pre>   1. put the attached test html &amp; image files in same place which your http   server can access them  2. open the test file via http protocol  3. when the image shows up  drag the image to somewhere like address bar    What is the expected output?  The URL of image will show up on the address bar. Or at least  Chrome   should not crash.    What do you see instead?  Chrome crashed       </pre>