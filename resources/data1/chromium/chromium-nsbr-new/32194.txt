<title>Issue 32194 -   chromium -    Add bookmark manager experimental extension API - Project Hosting on Google Code</title> <pre> Extensions/HTML5 are lacking some features that we need for creating the bookmarks   manager as an extension.    The following methods are the ones I think we need.    copy(listOfBookmarkId)  cut(listOfBookmarkId)  paste(parentId)  hasClipboardData(function(boolean))  onClipboardDataChanged(function(boolean))    getStrings(function(Object))    import()  export()    setDragData(listOfBookmarkIds)  getDragData(function(listBookmarkTreeNodes))    This needs to be [BookmarkTreeNode] since we can drag bookmarks and links from   other places.  onDragDataChanged.addListener(function(listBookmarkTreeNodes)) </pre>