<title>Issue 27997 -   chromium -    Bookmark sync new tab UI shouldn't show up in section menu - Project Hosting on Google Code</title> <pre>   1. Start Chromium built from head with --enable-sync  2. Open up a new tab  3. Click on the button on the new tab that looks like a down arrow        Expected:    Most visited  Recently closed  Tips    Actual:    Most visited  Recently closed  Tips  Bookmark Sync    Also  if the bookmark sync UI actually shows something  hiding and then   making visible it (via the menu mentioned above) doesn't actually re-show   it. </pre>