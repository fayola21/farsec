<title>Issue 44607 -   chromium -    chrome hangs on large &lt;pre&gt; pages - Project Hosting on Google Code</title>   1. go to the url (or any build log on Debian  the glibc ones are probably   very long ~1-2Mo  and are a very good stress test) ;  2. go to the bottom of the page (using &quot;page-end&quot;) ;  3. try to scroll back (using &quot;up&quot;)  4. see nothing scroll at all  and chromium proposing to kill the page that   has gone unresponsive        that it scrolls and isn't dog slow :)        <b>Please provide