<title>Issue 25827 -   chromium -    DCHECK in HostResolverImpl::RemoveOutstandingJob() - Project Hosting on Google Code</title> <pre> Mike reported getting a DCHECK here:    void HostResolverImpl::RemoveOutstandingJob(Job* job) {   JobMap::iterator it = jobs_.find(job-&gt;key());   DCHECK(it != jobs_.end());    &lt;---------------- HERE   DCHECK_EQ(it-&gt;second.get()  job);   jobs_.erase(it);  } </pre>