<title>Issue 26601 -   chromium -    Javascript executed twice - Project Hosting on Google Code</title> <pre> Chrome Version       : 4.0.223.11          Firefox 3.x: OK (according to someone else)           IE 8: FAIL      1. Put the attached code into an html file and run it in chrome  2. Resize the window (to trigger the code)  3. The code is executed twice. I did a lot of questioning around  but no-  one who nows JavaScript knows why this code is always executed twice in   Chrome and IE. It might be related to the way Chrome resizes  or it might   be an issue of the javascript engine.  The Javascript code is a valid code and the HTML doesn't matter  I also   tested it in XHTML 1.1 and still two alerts are shown        Executed once      Executed twice    The code:  &lt;html&gt;&lt;head&gt;&lt;script&gt;function addEventBeestje(elem  event  functie    geefDoor) {     if(elem.addEventListener) { //like it should be         elem.addEventListener(event  functie  geefDoor);     }else if(elem.attachEvent){ //IE only code        elem.attachEvent('on' + event  functie);     }else{ //if both previous functions are not supported        elem['on' + event] = functie;      } }  function laadfunctie()  { alert('resized'); }  addEventBeestje(window  'Resize'  laadfunctie  false);  &lt;/script&gt;&lt;title&gt;.&lt;/title&gt;&lt;/head&gt;&lt;body&gt;DitIsEenTestPagina&lt;/body&gt;&lt;/html&gt; </pre>