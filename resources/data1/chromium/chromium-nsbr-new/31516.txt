<title>Issue 31516 -   chromium -    No JavaScript call to trigger a heap snapshot - Project Hosting on Google Code</title>   1. Use for (var i in console) { console.log(console[i]); } to dump the list   of supported functions;  2. Note the lack of a function to take a heap snapshot (and I'm not sure   where else one would be.)        There should be a function such as console.takeHeapSnapshot        There is no such function.    <b>Please provide