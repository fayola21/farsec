<title>Issue 23385 -   chromium -    Move WebViewDelegate::RunFileChooser to WebKit::WebViewClient - Project Hosting on Google Code</title> <pre> Move WebViewDelegate::RunFileChooser to WebKit::WebViewClient    WebViewClient.h already contains a sketch of what this might look like.  Per   our discussion the other day  I think it makes sense to change runFileChooser   to return a bool  so that the consumer can just return false to indicate that   they will not run a file chooser dialog.  This way  the deletion of the   WebFileChooserCompletion can be handled by the caller of runFileChooser.    When defining WebFileChooserCompletion  please create a separate header file.    Please also avoid the STL container types.  Use WebKit::WebVector&lt;T&gt; instead   of std::vector&lt;T&gt; for instance  and use WebString instead of FilePath. </pre>