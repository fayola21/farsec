<title>Issue 42259 -   chromium -    Plugin process for npwrapper.libflashplayer.so crashes with shared library linux build - Project Hosting on Google Code</title> <pre>   1. Compiled a shared library chromium binary  (GYP_DEFINES=&quot;library=shared_library&quot;)  2. Fire up browser and point it to some page that uses flash (e.g. youtube  watch page)        Chromium should load the flash plugin and play the movie. Instead  the  plugin process crashes when invoking NPP_RetainObject()       </pre>