<title>Issue 32020 -   chromium -    Remove the hack to ignore SSL server certificate errors for SPDY. - Project Hosting on Google Code</title> <pre> Right now HttpNetworkTransaction::DoSSLConnectComplete() ignores  SSL server certificate error for SPDY:      if (IsCertificateError(result)) {      if (use_spdy) {        // We currently ignore certificate errors for spdy.        result = OK;      } else {        result = HandleCertificateError(result);      }    }    We should remove this hack when our SPDY test servers have real  certificates. </pre>