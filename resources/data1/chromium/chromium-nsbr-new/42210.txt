<title>Issue 42210 -   chromium -    Chrome should coalesce consecutive scrollwheel events - Project Hosting on Google Code</title> <pre> If a trackpad is used to scroll up or down quickly on a slow machine  Chrome   appears to end up with a backlog of scrollwheel ButtonPress events.  When   handling a scrollwheel event  Chrome should peek ahead in the event queue   for additional scrollwheel events and coalesce them all together into a   single WebKit event with a larger scroll distance.    Some info from evanm:    grep for  [&quot;scroll-event&quot;] to find the gtk event    WebInputEventFactory::mouseWheelEvent(event)) constructs the ipc'd  message which contains the delta pixels </pre>