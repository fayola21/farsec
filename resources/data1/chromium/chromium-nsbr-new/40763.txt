<title>Issue 40763 -   chromium -    Can display unnecessary browser action overflow menu. - Project Hosting on Google Code</title> <pre>   1. install multiple browser actions  2. drag handle over so some are in overflow menu  3. drag handle back very carefully one pixel at a time until all actions   are visible but overflow widget is also still visible    What is the expected output?    once all actions appear overflow widget should disappear    What do you see instead?    all actions and overflow visible at once       </pre>