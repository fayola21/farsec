<title>Issue 28751 -   chromium -    XMPP Sync will not work behind a proxy server - Project Hosting on Google Code</title> <pre> XMPP notifications are not hooked up to Chrome's proxy framework. Thus people   using bookmark sync behind a proxy server will fall back to polling  which   generates a larger amount of traffic  as well as slower synchronization. </pre>