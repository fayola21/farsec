<title>Issue 30687 -   chromium -    Use NSS on Windows instead of SChannel - Project Hosting on Google Code</title> <pre> In order to leverage SSL protocol feature work  we need to use an SSL stack   which where we can implement new features.  We also want to continue using   the OS certificate storage.    Implement NSS on Windows integrated with the Windows Certificate Store.   </pre>