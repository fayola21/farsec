<title>Issue 39437 -   chromium -    LocalStorage permission dialog should not block the WebKit thread. - Project Hosting on Google Code</title> <pre> LocalStorage permission dialog should not block the WebKit thread.  Right now it does because   that's the only thing running there.  The fix is to queue up all the requests for a particular origin if   that origin is waiting on a request. </pre>