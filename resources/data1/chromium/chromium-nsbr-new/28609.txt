<title>Issue 28609 -   chromium -    HTML dialogs need to share the same tab contents delegate across all platforms - Project Hosting on Google Code</title> <pre> Currently  the implementations for all three OSs have separate tab contents   delegates.  They need to be combined so that the behavior is consistent   across all platforms.  For example  only the OS X implementation explicitly   turns off history.   </pre>