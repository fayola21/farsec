<title>Issue 32107 -   chromium -    Zeroing a select element's options via JavaScript fails reliably when the DEVELOPER TOOLS are in use - Project Hosting on Google Code</title>   1. Load the page in a new chrome instance  2. Note that all select elements below are emptied  3. Open the DEVELOPER TOOLS: Page &gt; Developer &gt; JavaScript console  4. Note the console log entries  which detail correct operation  6. Refresh the page  7. Note that all select elements below still have options remaining (likely  3  5 and 7)  8. Note the console log entries  which detail incorrect operation  8. Closing the DEVELOPER TOOLS window reinstates correct behaviour      SELECT elements are reliably cleared.      SELECT elements are not reliably cleared whe the DEVELOPER TOOLS are in use.      <b>Please provide