<title>Issue 29741 -   chromium -    Date.getTime() crashes after a vetoed frame unload - Project Hosting on Google Code</title>   Put a page unload listener which does a popup to allow the user to veto a  frame unload.  After a vetoed unload  run the code (new Date()).getTime().   It will cause the JS thread to silently halt - no exception  it just  stops.  In our particular case the code which called getTime was started  from a timer which was set before the frame unload attempt and triggered  afterwards  but I'm not sure if that's important.      Get the time in ms      Silent JS thread halt    <b>Please provide