<title>Issue 23921 -   chromium -    Accessibility issue when using inverted colour scheme - Project Hosting on Google Code</title>   1. Set inverted color scheme (High Contrast Black in Windows 7)  2. Open Chrome Options dialog.        Text should be readable. UI fonts and colors should be customizable.      It seems there's a common mistake: one part (foreground color) is  hard-coded to be black and the background uses system color  so if you set  window background to black in windows there's no way to read text dialog  windows (and in the address bar too).    <b>Please provide