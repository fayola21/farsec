<title>Issue 29265 -   chromium -    Notifications should be restored if the user misses them! - Project Hosting on Google Code</title> <pre>   1. Bring up a notification  2. Exit Chrome  3. Launch Chrome again    Issue: There's no state between browser sessions. The notifications doesn't   come back once they are lost.    For example  if the browser crashes before the user has noticed/interacted   with the notification  it will never comeback. </pre>