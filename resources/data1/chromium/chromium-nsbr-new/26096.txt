<title>Issue 26096 -   chromium -    Get commands that return a result - Project Hosting on Google Code</title> <pre> Part of GL-based command buffers:    *) Get commands that return a result written.     To return a result requires synchronization. Once one of these commands is   written the rest can probably be auto generated.   </pre>