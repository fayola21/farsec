<title>Issue 38710 -   chromium -    Extension page action icon not updated correctly - Project Hosting on Google Code</title> <pre> Chrome Version       : 5.0.356.2 dev  URLs (if applicable) : N/A  OS version               : 10.5.8  Behavior in Chrome for Windows: not tested      1. Install the attached extension  2. Navigate to google.com  3. On the same tab  navigate to example.com      The page icon should change from the google favicon to a sad face when   example.com loads.      The icon does not change  unless the user clicks on the URL bar again.    This also happens if you go from example.com to google.com. If you use   the forward/back buttons  the wrong icon is used (it changes when the   back button is pressed  to the wrong one).     This does NOT happen on Chrome Mac 5.0.307.11 beta  so it looks like a   regression present in the dev version.    manifest.json  {    &quot;name&quot;: &quot;Page action icon tester&quot;     &quot;version&quot;: &quot;0.1&quot;     &quot;description&quot;: &quot;Page action bug&quot;     &quot;background_page&quot;: &quot;background.html&quot;     &quot;page_action&quot;: {      &quot;default_icon&quot;: &quot;sad.png&quot;    }     &quot;permissions&quot;: [      &quot;tabs&quot;    ]  }    background.js:  &lt;script&gt;  chrome.tabs.onUpdated.addListener(function(tabId  info  tab) {    if (tab.url.indexOf('example.com') != -1) {      chrome.pageAction.show(tab.id);    } else if (tab.url.indexOf('google.com') != -1) {      chrome.pageAction.setIcon({tabId: tab.id  path: 'google.png'});      chrome.pageAction.show(tab.id);    } else {      chrome.pageAction.hide(tab.id);    }  });  &lt;/script&gt; </pre>