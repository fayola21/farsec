<title>Issue 39533 -   chromium -    Select boxes do not broadcast an onChange event when value is changed in a minimized state - Project Hosting on Google Code</title>   1. Go to any page containing a select box (HTML select form element)  2. Click on the select box to open it and click it again to close it (the   focus is now still on the select box but it is in a minimized state)  3. Use the arrow keys on the keyboard to select any new value  4. Hit enter      The select box should remain minimized and the onChange event should be   broadcast so that JavaScript can &quot;do the right thing.&quot;      The select box expands and the onChange event is NOT broadcast.    <b>Please provide