<title>Issue 37732 -   chromium -    Opening Chrome TaskManager when NaCl Win64 broker process is running crashes the browser - Project Hosting on Google Code</title> <pre>   1. Load a page that uses NaCl on Win64  2. Open Chrome's task manager.    Expected: the broker process is shown in the Task Manager  Instead  the browser crashes.    The necessary support needs to be added for the broker process as we did for   the NaCl loader process. </pre>