<title>Issue 39869 -   chromium -    Content settings for WebGL - Project Hosting on Google Code</title> <pre> We need content settings for WebGL: ability to turn on/off  set exceptions for sites etc.  Additionally    its been suggested that we pop an infobar for each site to enable WebGL until it stabilizes  so the   content settings would need to have an option to &quot;prompt for each site&quot; </pre>