<title>Issue 35345 -   chromium -    [Extensions] The hint is missing when mouse pointer on browser action button in popup list - Project Hosting on Google Code</title> <pre>   1. Install some browser action extensions which has hint on UI button  2. Resize the browser action container to hide them  3. Click the chevron button on open the popup list  4. Move mouse pointer on an UI button which has hint      The hint should show  but now it doesn't show.       </pre>