<title>Issue 40881 -   chromium -    Sync setup dialog does not finish when an error is encountered - Project Hosting on Google Code</title> <pre> STR:    1. Start with a fresh profile.  2. Set up sync with a data type that is not enabled on the sync server you   have configured.  3. This will crash until <a title="Crash when enabling sync with a datatype that is not enabled on the server" class=closed_ref href="/p/chromium/issues/detail?id=40867"> bug 40867 </a> is fixed  but once it is it just sits   there forever.   </pre>