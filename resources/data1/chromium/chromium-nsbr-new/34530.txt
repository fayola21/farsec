<title>Issue 34530 -   chromium -    map areas of images are not updated when using ajax - Project Hosting on Google Code</title>   1. load a page that has an image and an area map that is applied over it    2. a partial update using Microsoft ajax to get a new picture and a new   area map    3. the new image is rendered correctly but the old area map is still used.   when you pass the mouse over it   you could see the old labels but not the   new ones      to display the new labels defined in the new area map      the old area map and labels are displayed instead    <b>Please provide