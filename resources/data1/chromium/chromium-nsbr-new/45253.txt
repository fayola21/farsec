<title>Issue 45253 -   chromium -    HTML5 Local Databases serialization (concurrency control) broken in Chrome 5 (extensions) - Project Hosting on Google Code</title>   1. Create an HTML5 local SQL database.  2. Perform two write queries on it from two different pages of an extension   (e.g. background + options).        Both queries are executed in arbitrary order (Chrome 4 behaviour).        The database hangs and any access to it results in non-responsive   behaviour  including trying to view the contents in the Dev tools window.   Neither the transaction nor the query error callback is called. CPU   utilization remains at 0. After 10-20 seconds  read access is restored but   attempting to perform any write queries results in the same hang.    <b>Please provide