<title>Issue 45804 -   chromium -    HTML form validation constraints should not be honored until UI is provided - Project Hosting on Google Code</title>   1. Visit:    data:text/html &lt;!doctype html&gt;&lt;form&gt;&lt;input   required&gt;&lt;input&gt;&lt;button&gt;Submit&lt;/button&gt;&lt;/form&gt;    2. Enter text into second field  leaving first blank  3. Submit        Either an error message is presented  or the form submits successfully.        The form does not submit  but there's no error message.  The first input is   focused  but that's a very subtle cue  and there's no way for the user to tell if   it was focused because it's required  didn't match a pattern  outside allowed   min/max/step  . . .    <b>Please provide