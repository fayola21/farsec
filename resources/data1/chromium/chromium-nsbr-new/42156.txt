<title>Issue 42156 -   chromium -    getComputedTextLength() on an SVG &lt;tspan&gt; element always returns 0 - Project Hosting on Google Code</title>   1. Open the attached file  test.xml  2. in the developer console  select the test &lt;tspan&gt; by running t =   document.getElementById(&quot;test&quot;);  3. check the output of  t.textContent    t.parentNode.getComputedTextLength()  and t.getComputedTextLength()        t.textContent returns the text inside the tspan  t.parentNode.getComputedTextLength() returns the length of the whole text   element  t.getComputedTextLength() returns the same as above  since the single   nested tspan holds all of the text        t.textContent returns the text inside the tspan correctly  t.parentNode.getComputedTextLength() returns the length of the whole text   element correctly  t.getComputedTextLength() just returns 0    <b>Please provide