<title>Issue 40415 -   chromium -    Support smb: URL-scheme - Project Hosting on Google Code</title> <pre> Chrome Version       : 5.0.360.4 (Official Build 42897) dev  URLs (if applicable) : smb://mycomputer/SharedDocs/    Chromium should support the SMB url-scheme by either:   1. rewriting smb: urls to file:// urls   (as smb://mycomputer/SharedDocs/ when represented as   file://mycomputer/SharedDocs/ works fine)  2. use the same code that file:// uses to support smb urls. </pre>