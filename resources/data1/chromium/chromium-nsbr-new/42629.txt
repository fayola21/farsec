<title>Issue 42629 -   chromium -    tabs.create brings Chrome application to front - Project Hosting on Google Code</title>   1. Create an extension that calls tabs.create after a short timeout   (several seconds).  2. Run the extension  start the timeout  switch to some other application    and wait.      After the timeout  a new tab should be opened in Chrome  so that it is   there when you switch from your other application back to Chrome.      The new tab is created  and Chrome steals focus from the current   application.    <b>Please provide