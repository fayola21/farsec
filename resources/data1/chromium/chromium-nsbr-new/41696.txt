<title>Issue 41696 -   chromium -    VerifyUpdatesCommand should handle tag conflicts better - Project Hosting on Google Code</title> <pre> Currently it handles it by generating a delete of the local node and an add of the server node.  This   makes it necessary to add extra logic to some change processors (e.g.  theme).    Making it simply rename the local node has some subtleties related to preserving invariants. </pre>