<title>Issue 41980 -   chromium -    ChromeFrameTestWithWebServer.FLAKY_FullTabModeIE_WindowOpenInChrome complains about a pure virtual call - Project Hosting on Google Code</title> <pre> Twice today we've seen chrome frame tests blow up the following:    [----------] 109 tests from ChromeFrameTestWithWebServer  [ RUN      ] ChromeFrameTestWithWebServer.FLAKY_FullTabModeIE_KeyboardTest  [       OK ] ChromeFrameTestWithWebServer.FLAKY_FullTabModeIE_KeyboardTest (5469 ms)  [ RUN      ] ChromeFrameTestWithWebServer.FullTabModeIE_FocusTest  [       OK ] ChromeFrameTestWithWebServer.FullTabModeIE_FocusTest (4109 ms)  [ RUN      ] ChromeFrameTestWithWebServer.FLAKY_FullTabModeIE_WindowOpenInChrome  <a href="/p/chromium/source/detail?r=6025">R6025</a>  - pure virtual function call    It seems the two times today it was with dmazzoni's cl (<a href="http://build.chromium.org/buildbot/waterfall/changes/14515">http://build.chromium.org/buildbot/waterfall/changes/14515</a>).  But it always clears   without a revert  so it must be something within the test or this cls reveals a dependency issue that causes something to not rebuild at first.   </pre>