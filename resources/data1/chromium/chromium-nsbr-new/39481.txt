<title>Issue 39481 -   chromium -    kTestWaitTimeoutMs in ui_layout_test.h is too low for worker tests under valgrind - Project Hosting on Google Code</title> <pre> UILayoutTest enforces a 60 second timeout on each test  which is too low for worker tests on   valgrind.    We need to allow this to be overridden via the command line. </pre>