<title>Issue 26511 -   chromium -    Clean up old FFmpeg linux binaries - Project Hosting on Google Code</title> <pre> Now with the ffmpeg.gyp change checked in we can whack all the old linux   binaries + DEPS rules. </pre>