<title>Issue 26232 -   chromium -    Bookmark import fails after forced first-run UI - Project Hosting on Google Code</title>   1. Trigger first run import UI  &quot;&quot;&quot;  To retrigger the first run UI (steps are untested  so use at your own risk):  * Quit Chrome.  * Delete the ~/Library/Application Support/Google/Chrome/First Run*   file.  * Restart Chrome.  &quot;&quot;&quot;  2. Work way through wizard to import bookmarks from Firefox (I did not   have Firefox browser open at this time)      That my existing bookmarks would be wiped clean and replaced with the   ones from Firefox.      The 'second' first-run import process finished without my existing   bookmarks being changed or overwritten. It was a false import.    <b>Please provide