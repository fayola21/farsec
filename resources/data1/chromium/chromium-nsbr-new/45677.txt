<title>Issue 45677 -   chromium -    IMG elements with SRC URLs that send a redirect can produce Content-Type warnings - Project Hosting on Google Code</title>   1. Create a URL on a webserver that returns: a Location header pointing to an image URL; and a   Content-Type: text/html header.  2. Create an HTML document with an IMG element with SRC attribute pointing to the created URL   from step 1.  3. View HTML document in Chrome.  4. Note the warning in the error console: Resource interpreted as image but transferred with   MIME type text/html.        Chrome should not issue the warning if the Content-Type header from the second  redirected   request specifies an image/ type.        Chrome issues a warning since the first request's Content-Type header does not specify an   image/ type.    <b>Please provide