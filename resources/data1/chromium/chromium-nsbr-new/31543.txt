<title>Issue 31543 -   chromium -    Send window geometry to plugin via IPC - Project Hosting on Google Code</title> <pre> Cocoa Mac plugins need the ability to convert between plugin  window  and screen coordinates. To   do that  we need to know the full window geometry  rather than just figuring out the upper-left   offset based on mouse events  which means we need to send window geometry information via IPC. </pre>