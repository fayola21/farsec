<title>Issue 34942 -   chromium -    Task Manager maxes CPU use on large chrome sessions - Project Hosting on Google Code</title>   1. Create a large session (many windows/tabs).  2. Hit Shift-ESC to start the Task Manager  3. Watch CPU use go through the roof (hits 100%).      Task manager should scale its refresh rate to some combination of (a) the  session size (rough); or (b) its impact on the machine (better).      CPU use goes to 100% and the chrome windows/tabs become very sluggish (I  presume other processes on the system are similarly impacted).    <b>Please provide