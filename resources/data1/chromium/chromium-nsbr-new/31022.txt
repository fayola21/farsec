<title>Issue 31022 -   chromium -    Create a more simplistic delete method for Windows build and build.dead deletion - Project Hosting on Google Code</title> <pre> Please add a method that shells out the delete for Windows to avoid   encountering future delete issues on gclient checkout like we encountered   when the Mac reference build into trunk (i.e. issue w/ a limitation in the   size of files we could delete  which caused failures in the cleanup parts   of the gclient checkout).      Maybe something like:    def Deltree(root):    &quot;&quot;&quot;Removes a given directory.      Args:      root: (string) The directory to (recursively) remove.    &quot;&quot;&quot;    if not os.path.exists(root):      return      if sys.platform == 'win32':      os.system('rmdir /S /Q ' + root.replace('/'  ''))    else:      for name in os.listdir(root):        path = os.path.join(root  name)        if os.path.isdir(path):          Deltree(path)        else:          os.unlink(path)      os.rmdir(root) </pre>