<title>Issue 42862 -   chromium -    Sync users that used Autofill are now throttled - Project Hosting on Google Code</title> <pre> If a user tried to use autofill sync  they may have bumped into throttling. Numerous users are   reporting being throttled when autofill was turned on  and can no longer sync bookmarks. </pre>