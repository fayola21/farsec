<title>Issue 29824 -   chromium -    Heap  stack and more contain read write executable permissions under linux - Project Hosting on Google Code</title>   1. ps -Af | grep chrome  2. pgrep -l chrome | cut -f 1 -d &quot; &quot; | xargs -i cat /proc/{}/maps | less  3. Search for rwx permissions        There should be no or only a small number of maps that have read  write and   executable.  These permissions allow for much easier memory corruption   exploitation  especially a rwx HEAP.  An attacker can simply use javascript   to perform a heap spray to defeat address space layout randomization.    <b>Please provide