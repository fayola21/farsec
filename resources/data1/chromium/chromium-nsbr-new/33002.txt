<title>Issue 33002 -   chromium -    Settings and plugins are user independent...? - Project Hosting on Google Code</title>   1.Open Google Chrome in Windows under any user's account.  2.Install plugins  change settings  ect.  3.Switch to another user's account and you'll notice that  even when an admin   alters the settings  Google Chrome acts separately for each user. Is this a   feature  or can this be changed so that the browser will install on a system   wide basis  where changes affect all the users?      That the settings of the admin in Windows will apply to all other user's   settings.      When Google Chrome is installed  the current user that installed it can only   modify settings for themselves  and other users have to modify it on their own   as well.    <b>Please provide