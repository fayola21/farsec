<title>Issue 30526 -   chromium -    Tab crashes when rendering SVG pattern with unknown height and width - Project Hosting on Google Code</title>   1. Load the svg_working link above  nothing will be displayed but the   browser will be fine.    2. Load the svg_failing link above  the tab will crash.    Using JS libraries I've created SVG which correctly applies the URL pattern   (and displays it) when height and width are set  and crashes when they are   not.      Not crashing      Crashing    <b>Please provide