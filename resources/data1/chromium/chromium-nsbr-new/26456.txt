<title>Issue 26456 -   chromium -    Canvas toDataURL image/png is not a correct encoding when using alpha channel - Project Hosting on Google Code</title>   1. Create a canvas with any sort of drawing that has transparency (fillStyle(240 254 211 0.7))  2. Get the dataurl for that canvas. (canvas.toDataURL(&quot;image/png&quot;))  3. Compare the actual canvas to the image generated by toDataURL. They are not the same. (The dataURL seems darker is many   cases)      The image generated by toDataURL should be identical to the canvas rendered on screen.      The encoded data from toDataURL does not produce the same image that is displayed on the canvas.    <b>Please provide