<title>Issue 38088 -   chromium -    Match Firefox's per-host connection counting logic for persistent connections - Project Hosting on Google Code</title> <pre> Firefox has a separate per-host limit for persistent vs non-persistent (i.e.   Connection:close) HTTP connections.  We should match this behavior so apps   can open transient communication channels without hitting the general per-  host limits. </pre>