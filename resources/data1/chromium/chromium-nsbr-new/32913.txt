<title>Issue 32913 -   chromium -    Update extensions API to accommodate phantom tabs - Project Hosting on Google Code</title> <pre> As part of 32845 we're adding a new state for tabs: phantom. This happens if   you attempt to close a pinned application tab (currently a pinned tab is   considered an app tab). There are likely a couple of additions to the   extensions API we should consider:    . Event when a tab is made phantom.  . chrome.tabs.get status should include phantom.   </pre>