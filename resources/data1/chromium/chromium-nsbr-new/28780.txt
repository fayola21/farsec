<title>Issue 28780 -   chromium -    Chrome on OS X uses  ~Library/Application Support/Google/Chrome/Default/Cache instead of ~Library/Caches causing consistently large Time Machine backups - Project Hosting on Google Code</title>   1. Use Time Machine  2. Note that all hourly backups are 200+ MB  3. Note that Chrome isn't using ~Library/Caches        Time Machine ignores everything in ~Library/Caches so if it was used there   wouldn't be large backups.        200+ MB backups every hour    <b>Please provide