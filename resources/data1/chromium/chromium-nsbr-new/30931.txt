<title>Issue 30931 -   chromium -    Images fetched by javascript Image() object corrupt cache - Project Hosting on Google Code</title>   1. Go to a page that prefetches images using javascript Image() objects.  2. Navigate away from the page while the images are being downloaded        Images should be viewable  whether or not the prefetches succeeded.        Images that were not successfully prefetched are stuck in a broken image   state. Reloading a page with the image using shift-refresh does not resolve   the problem. Clearing the cache through the Options menu does resolve the   problem.    <b>Please provide