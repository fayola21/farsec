<title>Issue 38821 -   chromium -    Tabs appear offscreen when few and window resized (xmonad) - Project Hosting on Google Code</title> <pre> This applies to the new right-to-left tab strip.    Few tabs  failing case  1. Open few tabs (1-3) so that a window resize won't affect tab size  2. Resize the window to half its size (xmonad ctrl-space  changing layout)  3. Observe that the tab strip is empty (sceenshot)    Many tabs  no failure  1. Open many tabs (more than 3) so that a window resize will change tab size  2. Resize the window  3. Observe that the tab strip appears correctly   </pre>