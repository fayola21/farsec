<title>Issue 35411 -   chromium -    Chrome crashes when (multiple?) background gradients with color-stop are present on a page - Project Hosting on Google Code</title>   1. Keep refreshing a popup (done using window.open) that my extension   opened that has CSS rules with color stops in a background gradient.  It it will also crash if I add a second element to the page with a gradient   with a color stop (in my screen shot  it happens when I expand my right   pane)      Should work      Extension crashes    <b>Please provide