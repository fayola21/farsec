<title>Issue 35907 -   chromium -    OfficialBuildFactory needs to cleanup windows support - Project Hosting on Google Code</title> <pre> OfficialBuildFactory has a gate on building to trim what is built on windows    seems like this could get revisit to use the existing support to build a   project/target to get that subset instead of special hacking within the factory   like this. </pre>