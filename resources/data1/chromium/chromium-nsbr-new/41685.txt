<title>Issue 41685 -   chromium -    div style height - Project Hosting on Google Code</title> <pre> Chrome Version       : 5.0.376.0 (44292) Ubuntu                    1. Create a div  2. Assign a style height attribute of 100%      The div takes the 100% height of the div's wrapper      The div doesn't renders the established style rule.    The source code show the applied style and the html inspector doesn't show   it. When the attribute is applied using the developer tools then the   browser renders the height style attribute. </pre>