<title>Issue 25261 -   chromium -    Autocomplete window can not be triggered by clicking if the window has scrolled - Project Hosting on Google Code</title> <pre>   1. Load a long page with a text box (like crbug.com)  2. Scroll the the page down some  3. Type in the box and then click twice to trigger the autocomplete      Expect the autocomplete window to popup with suggestions.  Instead nothing   happens.  If the page is scrolled all the way up  the autocompletion   suggestions do appear.   </pre>