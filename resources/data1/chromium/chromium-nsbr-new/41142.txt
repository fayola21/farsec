<title>Issue 41142 -   chromium -     Memory increasing when calling chrome.tabs.getSelected() from timer handler in BG page.  - Project Hosting on Google Code</title> <pre> Chrome Version       : 4.1.249.1045 (42898)        In my extension for Google Chrome I have backgound page with timer  when  timer fires I need to get selected tab of current window.  When calling chrome.tabs.getSelected() function from timer handler - I  see in Task Manager (both of GC &amp; Windows one) that memory of  extension increasing. When timer works without calling calling  chrome.tabs.getSelected()  - than all is fine &amp; memory not increasing.  I think it's a bug of Google Chrome.    Here is the sample code which reproduce this problem:    &lt;html&gt;  &lt;script&gt;    function spAutofillEngineOnTimer()  {    try    {      // obtain selected tab      chrome.tabs.getSelected(null         function(selectedTab)        {          // .. call some code          // start timer          spStartAutofillEngineTimer();        }      );    } catch (ErrorMessage)    {      alert('spAutofillEngineOnTimer() Error: ' + ErrorMessage);    }    }    function spStartAutofillEngineTimer()  {    try    {      setTimeout('spAutofillEngineOnTimer()'  50);    } catch (ErrorMessage)    {      alert('spStartAutofillEngineTimer() Error: ' + ErrorMessage);    }    }    spStartAutofillEngineTimer();    &lt;/script&gt;  &lt;/html&gt;      No memory increasing for extension process.      Memory increasing for extension process on each timer event occured. </pre>