<title>Issue 31204 -   chromium -    Google Chrome deb installer should ask whether to install the apt repo using debconf - Project Hosting on Google Code</title>   1. Install Google Chrome from a downloaded Deb file on Ubuntu.  2. The installer does not ask whether to add the update repository        The installer should present a prompt via debconf asking the user if she   would like to enable the Google Chrome repository (with a default of   &quot;yes&quot;).        Instead  the user must do &quot;sudo touch /etc/default/google-chrome&quot; or some   such in order to prevent the repo from being added. Not everyone knows how   to manually create files in system directories  so this is sub-optimal.    <b>Please provide