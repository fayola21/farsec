<title>Issue 25843 -   chromium -    Kill / yank buffer doesn't work - Project Hosting on Google Code</title> <pre> Chrome Version       : 4.0.223.8    OS version               : 10.5.8  Behavior in Safari 3.x/4.x (if applicable): works  Behavior in Firefox 3.x (if applicable): n/a  Behavior in Chrome for Windows: n/a      1. Now that Chrome-the-magnificent honors the default emacs-style  keybindings in text editors  get rid of some editable text (e.g. in the  emacs compose window) by typing Ctrl-k  2. Try to reintroduce that text by typing Ctrl-y        * The text pops back out        * Nothing    Note that this IS NOT a copy / paste issue  or at any rate it does not use  the same clipboard that the Cut / Copy and Paste menu commands use. This is  a separate mechanism that works in all Cocoa apps.   </pre>