<title>Issue 27241 -   chromium -    'gcl change --force' fails when it is in read-only checkout. - Project Hosting on Google Code</title> <pre> 'gcl change --force' should not fail.  It should generate a random change name as before even if in read-only   checkout     Note: If a change name is given to gcl  e.g. 'gcl change mychangename --  force'  it doesn't fail.    This issue is related with:  <a href="http://codereview.chromium.org/378027">http://codereview.chromium.org/378027</a>   </pre>