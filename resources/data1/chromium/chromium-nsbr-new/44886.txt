<title>Issue 44886 -   chromium -    why the same extensions on mac uses three times the memory they uses on windows? - Project Hosting on Google Code</title> <pre> Chrome Version       : 6.0.408.1 dev  OS version               : 10.6.3  Behavior in Chrome for Windows: three times less the memory they uses on   mac :)         1. install any extension on both version of chrome (win &amp; mac)  2. open the chrome's task manager and monitor the memory usage          same memory consumption        the _same extension_ on mac uses three times the memory it uses on windows   </pre>