<title>Issue 32923 -   chromium -    html5 drag&amp;drop regression - Project Hosting on Google Code</title> <pre> The following user script works in 4.0.295.0 and no longer does in 4.0.302.2:    window.addEventListener(&quot;dragenter&quot;  function(aEvent) { aEvent.returnValue   = false; }  false);  window.addEventListener(&quot;dragover&quot;  function(aEvent) { aEvent.returnValue =   false; }  false);  window.addEventListener(&quot;drop&quot;  function(aEvent) {   alert(aEvent.dataTransfer.getData(&quot;Text&quot;); }  false);    Steps to reproduce:  1. Create a user script with the above content.  2. Open any web page  select some text and drag&amp;drop it onto the web page   itself.    Expected result:  The &quot;dragging/copying&quot; pointer appears at most a second after you start the   drag (and the dragged text is alert'd when dropped).    Actual result:  The &quot;dragging/copying&quot; pointer doesn't appear or at most just for the blink   of an eye and dropping doesn't trigger the alert.    Note: Replacing |aEvent.returnValue = false;| with   |aEvent.preventDefault();| doesn't work  either. </pre>