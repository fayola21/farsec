<title>Issue 27570 -   chromium -    Omnibox/toolbar M5 updates: reload/star shuffle  lock move  icon modifications  hide http:// - Project Hosting on Google Code</title> <pre> Behavior:    Icon is magnifying glass for empty field    Icon changes while user types  queries show mag  urls show globe  https   show globe until connection is established    Clicking the proxy icon shows page info    Dragging the proxy icon drags the url    This replaces the HTTPS indicator on the right of the location bar    HTTP urls strip &quot;http:// but https is still shown    Google searches are shown using the original query  not the google url.    Other changes:  Omnibox dropdown is aligned with omnibox  not the Star and go buttons.     </pre>