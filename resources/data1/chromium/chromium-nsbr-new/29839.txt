<title>Issue 29839 -   chromium -    Add a per-data type ChangeProcessor dispatch mechanism (for syncapi-&gt;chrome) - Project Hosting on Google Code</title> <pre> A map/registry of mime-type to to ChangeProcessor might be all we need  so   long as it is configurable and updatable in response to (for example) UI   allowing changing the actively synced types. </pre>