<title>Issue 32217 -   chromium -    Fix the TranslatorTest.TranslatePages unit test on Unix - Project Hosting on Google Code</title> <pre> The TranslatorTest.TranslatePages currently does not work on Unix and  should be fixed.    That test runs a render view  loads some HTML contents (with sub-resources  in it).  It DCHECKs today because the IPC channel implementation on Unix  requires a valid connection when creating a client channel.   </pre>