<title>Issue 38677 -   chromium -    File:// links and image sources do nothing - Project Hosting on Google Code</title>     1. Create an image and store it locally or on a file server.  2. Create a page with an A tag that has HREF or an IMG tag with SRC   pointing to that image in the form of file://servername/sharename/image.jpg  3.  Browsing using HTTP (e.g.  your page is on a server; it works locally   if you're browsing the page locally as well.  See if you can click the link   and get to the file  or if the image displays.          Link leads you to the file  image displays.        Link is not clickable  image does not display.    <b>Please provide