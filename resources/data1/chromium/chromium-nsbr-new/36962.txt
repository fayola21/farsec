<title>Issue 36962 -   chromium -    HTML5 forms validity API not properly implemented for required radio groups - Project Hosting on Google Code</title>   1. Create an input group of the type 'radio' with @required set.  2. Check one of the boxes.  3. Call widget.checkValidity()      If any box in the group is checked  the whole control is valid.    widget.checkValidity() should return true.      widget.checkValidity() always returns false if a radio group is required  and   causes document.forms[0].checkValidity() to also return false.  It does this even   if a button is checked (which defies the spec)  or if the button you call it   against is checked (which defies logic).      <b>Please provide