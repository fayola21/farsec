<title>Issue 29646 -   chromium -    Browser Id not properly assigned to RenderView objects for extension pop-up pages in Chrome-Frame - Project Hosting on Google Code</title> <pre> ChromeFrame version: 33757    Related URL(s): N/A      1.  Create an extension that uses chrome.experimental.popup.show(...)  and   chrome.experimental.extension.getPopupView().  2.  Launch Chrome-Frame  and navigate to an extension page that displays a   popup  and calls chrome.experimental.extension.getPopupView().  3.  Observe the browser ids passed in to   ExtensionImpl::PopupViewFinder(...)  and   ExtensionViewAccumulator::Visit(...).        The browser_id property returned from iteration through the render_host set   should return the same browser id extracted at the call site to   PopupViewFinder.        The browser_id property of the popup within the iterated views is -1  but   the id extracted from the following snippet (in PopupViewFinder) is valid   (not -1).        RenderView* render_view =   bindings_utils::GetRenderViewForCurrentContext();      int browser_window_id = render_view-&gt;browser_window_id();   </pre>