<title>Issue 37987 -   chromium -    Render JSON nicely (and correctly) inside browser - Project Hosting on Google Code</title>   1. Currently  Firefox has an extension called JSONView that pretty prints JSON. This makes is   much easier to visually view JSON.  2. Try the url above with the extension installed in FF to see the expected result  3. Try the same url in Chromium        Expected result is pretty printing of JSON          a) result is not pretty printed  b) more importantly  it is not correctly parsed as the &lt;h1&gt; causes a problem in how the json   output is rendered.      <b>Please provide