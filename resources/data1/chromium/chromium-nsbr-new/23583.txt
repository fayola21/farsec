<title>Issue 23583 -   chromium -    Extension API inconsistency with ports - Project Hosting on Google Code</title> <pre> Our 2 ways of opening channels is inconsistent:    int chrome.extension.connect(string name);    int chrome.tabs.connect(int tabId  object with string name);    We should change the former to:    int chrome.extension.connect([string extensionId]  object with string   name);    If extensionId is not specified  it should default to the current extension. </pre>