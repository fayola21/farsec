<title>Issue 32802 -   chromium -    [Extensions] When chrome window is not maximazed some extensions' UI button are not shown - Project Hosting on Google Code</title> <pre>     1. Install some extensions which have browser action UI button  2. Narrow down the chrome window        As the window narrows down  some extensions' UI button disappear and no way  to find them.     There should be a &quot;more&quot; button shown when some extensions UI button are  hided  so that user can click it to find those hided buttons. Check  screen-shot for reference.       </pre>