<title>Issue 31347 -   chromium -    Clicking on a link sometimes fails to create a new tab. - Project Hosting on Google Code</title>   1. Browse to certain pages where clicking on a link will create a new tab.  2. Click on a link.  3. Click on another link.      A new tab should get created in step 2  and another new tab in step 3.      Sometimes rather than create a new tab  an existing tab (not the current   one  though) gets reused. Specifically  in step 3  if step 2 creates a new   tab  that new tab should not be reused in step 3. Step 3 should create its   own new tab if step 2 did.    <b>Please provide