<title>Issue 40578 -   chromium -    The shadow fills in the whole circle when using the canvas arc to draw a circle outline (stroke) - Project Hosting on Google Code</title>     Drawing a circle to the canvas using the .arc function and stroke to only  draw an outline. If you have a shadow color set then the whole inside of  the circle is shadowed instead of just the outline circle.      The shadow should only appear on the circle outline and not fill the inside  of the circle.      The whole inside of the circle is filled in with the shadow color    <b>Please provide