<title>Issue 35464 -   chromium -    Notifications are in the wrong place when desktop is resized - Project Hosting on Google Code</title> <pre>   1. start chrome  2. resize desktop  e.g.  go to presentation mode  3. create notifications        notifications are created in the wrong spot because the UI is still using  the old desktop metrics  until you restart the browser.       </pre>