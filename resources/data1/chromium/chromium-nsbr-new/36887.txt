<title>Issue 36887 -   chromium -    user scripts : @include meta not respected when used with tabs - Project Hosting on Google Code</title>   1. Create a user script with a metadata @include  2. Separate @include and your url pattern with one or several tabs  3. Install the user script      The script should load only on urls matching the pattern      The script loads on every page    If tabs are replaced by spaces  the @include meta works well      <b>Please provide