<title>Issue 35362 -   chromium -    Box-shadow: inset doesn't render properly - Project Hosting on Google Code</title>   1.Create an element with -webkit-box-shadow: inset as a css property    either on top or bottom or either side. Can have border-radius  and other   shadows defined      To have a shadow on the inside portion of an element  for creating embosses. Shadow must follow curve of border-radius.       * Shadow can render outside box (as if inset didn't even apply)   * Shadow renders inside box properly  but extends through border-radius    without rounding    <b>Please provide