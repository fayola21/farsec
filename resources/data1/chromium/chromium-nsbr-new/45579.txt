<title>Issue 45579 -   chromium -    need scalable graphics - Project Hosting on Google Code</title> <pre> We need scalable graphics for linux and windows in order to make the UI   scalable (currently you can't really control the font size of chrome's   chrome because the size of all the background graphics is hardcoded).     I suspect this might have been filed previously  but I couldn't find it.    I think Mac already has this to some extent (?)    keywords: vector image images scale </pre>