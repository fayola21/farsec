<title>Issue 39623 -   chromium -    Why are pinned tabs broken all of a sudden? - Project Hosting on Google Code</title>   1. Right Click and pin tab.  2. Try dragging another tab to the left to 'auto-pin' tab  but nothing   happens.  3. Close a pin tab using mouse wheel click  the tab doesn't close as it   should      Tabs should auto pin if dragged all the way to the left  if a pinned tab is   already present. And a pinned tab should close when wheel mouse clicked.      Tabs don't pin when dragged to the left. Pinned tabs don't close on first   attempt    <b>Please provide