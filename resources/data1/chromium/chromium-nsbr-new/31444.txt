<title>Issue 31444 -   chromium -    chrome.tabs.onUpdated never passes in url sometimes on Google Voice - Project Hosting on Google Code</title>   1. Set up an extension with a handler for chrome.tabs.onUpdated  2. The handler should output changeInfo.url somehow (alert  for example)  3. Load Google Voice  and then refresh to load it again.        Both times the loaded url should be output at least once during its   loading.        Works fine the first time  but for all subsequent times in the session  url   is undefined on changeInfo.    <b>Please provide