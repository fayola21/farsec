<title>Issue 42646 -   chromium -    net::ProxyResolverV8::Context::DnsResolveCallback() sometimes resolves a hostname of &quot;null&quot; - Project Hosting on Google Code</title>   1. Run any chrome sync integration test on a windows machine    What is the expected result? Test passes because the machine is on the  *.google.com domain.    What happens instead? gethostname() returns only part of the computer name  (&quot;hostname&quot; instead of &quot;hostname.ad.corp.google.com&quot;). This causes the  match to fail for the DNS exception for *.google.com  causing the test to fail.    <b>Please provide