<title>Issue 30487 -   chromium -    Tooltip max length clip should be in shared code - Project Hosting on Google Code</title> <pre> We cap the length of tooltips in a Windows-specific file  to protect against   a page DOS'ing the user:    chrome/browser/renderer_host/render_widget_host_view_win.cc:  //   accidentally DOS the user with a mega tooltip (since Windows doesn't seem    Turns out this crashes us on GTK too.  I bet it affects Mac as well.    Test case attached.  Hover over the text. </pre>