<title>Issue 40961 -   chromium -    Regression: &lt;select&gt; drop down box persists when click on tabstrip - Project Hosting on Google Code</title> <pre> Tested: Windows Vista      1. open news.google.com  2. click the language selection box on top-left of the page  3. then  click browser tabstrip area (not the specific tab area)  and move   the browser window around      the language &lt;select&gt; box should disappear when click on browser tabstrip   area.  Instead  the &lt;select&gt;  drop down box persists. And the drop-down box   position persists  which is shifted from the &lt;select&gt; element box position.      Works correctly in 40366  but not 43461.   </pre>