<title>Issue 36201 -   chromium -    Extensions needs an way to determine if users are idle  (idleService) - Project Hosting on Google Code</title> <pre> Firefox has an easy way to determine if a user is idle:    <a href="https://developer.mozilla.org/en/nsIIdleService">https://developer.mozilla.org/en/nsIIdleService</a>    Chrome needs something similar.     This was important enough to LastPass that we made an NPAPI plugin just to  get the idle status.  We use it to allow people to automatically logoff  when they've gone idle.   </pre>