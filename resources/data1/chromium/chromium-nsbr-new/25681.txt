<title>Issue 25681 -   chromium -    r29152 added bad operations to FilePath - Project Hosting on Google Code</title> <pre> <a href="http://codereview.chromium.org/267051">http://codereview.chromium.org/267051</a>    Line 161: bool FilePath::AppendAndResolveRelative(const FilePath&amp;   relative_path   This is wrong and we need to remove it.    We can't resolve .. without referencing the disk.  (Think symbolic links.)    We can't reference the disk in FilePath.    If you need to resolve ..  and you need to hit the disk  you need something in  file_util  not file_path. </pre>