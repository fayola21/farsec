<title>Issue 23884 -   chromium -    Clicking between omnibox and renderer results in repainting most of the window - Project Hosting on Google Code</title> <pre>   1.Click on the renderer  2.Click on the omnibox  3.notice we end up repainting nearly the whole window    This happens because the focusmanager detects the focus change and invokes   SchedulePaint on both widgets  which results in almost the whole window   repainting. </pre>