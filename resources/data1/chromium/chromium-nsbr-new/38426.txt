<title>Issue 38426 -   chromium -    Add &quot;type&quot; parameter to chrome.extension.getViews() - Project Hosting on Google Code</title> <pre> Currently  we have chrome.extension.getBackgroundPage()  getExtensionTabs()    and getViews().    We're going to keep adding more view types. Currently we have notification   windows and infobars already in progress.    Instead of keep adding more of these functions  we should just add an   optional 'type' parameter to getViews() and deprecate getExtensionTabs().    </pre>