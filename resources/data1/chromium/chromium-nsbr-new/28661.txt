<title>Issue 28661 -   chromium -    Learn more link from FRUI doesn't open when chrome is default browser. - Project Hosting on Google Code</title>   1. Install Chrome and make default browser  2. Delete the profiles directory ~/.config/google-chrome  3. Re-launch Chrome which will bring up the FRUI  4. Click on the &quot;Learn more&quot; link        Page will open       On the console  there reads  &quot;created new window in existing browser session&quot;    I'm not sure what should be the behavior here as Chrome is technically   still the default browser and so chrome should start - but it wouldn't   start since we're still at the FRUI.    ** If default browser is set to some other browser  then clicking on link   opens the page in the default browser.    <b>Please provide