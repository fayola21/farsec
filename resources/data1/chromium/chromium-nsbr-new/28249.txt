<title>Issue 28249 -   chromium -    dragleave not fired on failed drag - Project Hosting on Google Code</title>   1. Have a page that is selective about where it accepts drops  e.g. one   place where it sets the dropEffect to 'copy' and the rest it sets it to   'none'  2. Drag a file from the desktop into the window and drop it outside the   target that would accept it.        No drop event should be generated  but a dragleave event should be fired on   the current target.      No event is fired    <b>Please provide