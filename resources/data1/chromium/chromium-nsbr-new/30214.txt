<title>Issue 30214 -   chromium -    Chrome hangs trying to upload an .app in Mac OS X - Project Hosting on Google Code</title>   1. Create a simple file upload form as explained in the url.  2. Load Chrome in Mac OS X 10.6.2  3. Pick an application (.app) and try to upload it      -Not allowed to upload the .app as it is a folder  -Or compress it in a .zip as Safari and upload that      Chrome shows a dialog stating that a page isn't responding. The &quot;close  page&quot; button in that dialog doesn't close the tab  but although it can be  manually closed  then Chrome won't allow to do a clean exit. It requires a  forced Quit.    <b>Please provide