<title>Issue 37293 -   chromium -    With RTL UI  items in download shelf can't be hidden correctly when shrinking the browser window. - Project Hosting on Google Code</title> <pre>   1. Launch chrome with a RTL UI  2. Download some files to make the download shelf visible.  3. Shrink the browser window      The download items should be hidden when the browser window is too narrow to   show them. But actually the items can't be hidden correctly.         </pre>