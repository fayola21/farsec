<title>Issue 40052 -   chromium -    Mode_switch directional keysymbols used when Mode_switch key is not depressed (Linux) - Project Hosting on Google Code</title>   1. Create a custom key mapping which includes a Mode_switch directional  value.  For example  on a US keyboard with the 'J' key being code 44  you  could run:  $ xmodmap -e 'keycode 44 = h H Left'  2. Try to use a lower-case 'h' in an HTML text field in the browser (NOT  the URL bar).      An 'h' is printed.        Nothing is printed (it's treated as a left-arrow).      <b>Please provide