<title>Issue 37192 -   chromium -    Scripts abort with no message on stack overflow (including when run in debugger) - Project Hosting on Google Code</title>     When a script overflows the stack  the script is aborted with no message   (even in the built in debugger).  All other errors add an error to the   error list. (and a corresponding red warning indicator at the lower   right corner of the browser window).          A stack overflow error added to the debugger error list.        The script silently stops running.    <b>Please provide