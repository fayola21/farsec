<title>Issue 45857 -   chromium -    Password import is delayed by ~30 seconds on first run - Project Hosting on Google Code</title> <pre>   1. Run Chrome as first run  import passwords.  2. Immediately look for imported passwords  by going to a login page or in   the Options dialog.  3. See them not.  4. Wait ~30 seconds.  5. Refresh the login page  or open the &quot;Saved Passwords&quot; dialog again.  6. There they are.       </pre>