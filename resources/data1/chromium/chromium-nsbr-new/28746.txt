<title>Issue 28746 -   chromium -    Google Chrome Linux counts killing the browser as a crash - Project Hosting on Google Code</title> <pre>   1. Start Chrome from a terminal  let it run for a while so the   cleanly_exited = false key/value pair gets written to disk.  2. Kill Chrome with Ctrl+C        Metrics service count this as a crash  even though we didn't actually crash. </pre>