<title>Issue 37294 -   chromium -    Custom HTTP Header Issue - Project Hosting on Google Code</title>   1. Create SWF file which makes HTTP POST request on some url.  2. Add custom HTTP header to request (e.g. CustomHeader: CustomValue)  3. Use network protocol analyzer (e.g. Wireshark) if required to see the actual packet content.      HTTP header name will be sended in case-sensitive manner (e.g. CustomHeader: CustomValue)      HTTP header name will be sended in lowercase (e.g. customheader: CustomValue)    <b>Please provide