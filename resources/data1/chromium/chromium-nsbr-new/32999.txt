<title>Issue 32999 -   chromium -    Z-Index Issue - Project Hosting on Google Code</title>   1. Create a tabbed menu with an unordered list with z-index on the inactive   tabs of 700. Create another a element for the active class and set it's z-  index to 900. Create an a:hover with a z-index of 800 for the inactive   tabs. Above this  create a closed div for use as a login bar. It's z-index   is 9999 as it's always above the tabs.  2. Open it in Chrome and hover over your navigation. Nothing happens.   Navigation is not clickable.      Expected the hovered elements rises above the inactive elements but below   the active tab and is a clickable link.        Nothing.      <b>Please provide