<title>Issue 38091 -   chromium -    GTalk auth failures should be exposed to the UI - Project Hosting on Google Code</title> <pre>   1. Log into sync  2. Change sync account password  3. Restart browser        Gtalk auth fails  so notifications are disabled.  This should be exposed in the UI somehow  e.g.   generate a &quot;Login failed&quot; error.    (Also  see <a title="GaiaAuth must die" class=closed_ref href="/p/chromium/issues/detail?id=38034"> issue 38034 </a>) </pre>