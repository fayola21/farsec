<title>Issue 37222 -   chromium -    Keygen unit test should use a temporary keychain - Project Hosting on Google Code</title> <pre> We had some trouble with the new KeygenTest failing due to mysterious Keychain errors. Rebooting   the build bot cleared that up  but it would probably be more reliable to have the test create a   temporary keychain  use it  and then delete it. </pre>