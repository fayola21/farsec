<title>Issue 24660 -   chromium -    No onmouseout handlers called when cursor leaves tab content area - Project Hosting on Google Code</title>   1. Navigate to a page with an object that responds to onmouseover and   onmouseout events  and is positioned near the edge of the window.  2. Move the mouse pointer inside the object  putting it into its mouse-over   state.  3. Move the mouse pointer outside the window  or outside the tab's content   area.        The object's onmouseout handler should get called  putting it into its   normal state  not the mouse-over state.        No event is triggered until the next time the cursor appears anywhere   within the tab's content area.    <b>Please provide