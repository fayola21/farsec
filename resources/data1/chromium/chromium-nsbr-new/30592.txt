<title>Issue 30592 -   chromium -    Extensions removing tabs can cause crashes if done inside nested event loops - Project Hosting on Google Code</title> <pre>   1.Load the attached extension.  2.Drag the newly created tab and do not release. (Do not drag out of the   window)  3.Wait for the extension to close the tab.      EXPECTED: Tab closes.  ACTUAL: Tab closes and crash. </pre>