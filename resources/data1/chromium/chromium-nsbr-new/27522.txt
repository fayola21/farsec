<title>Issue 27522 -   chromium -    Mac: Open All Bookmarks (and variants) in a bookmarks folder's context menu opens *all* bookmarks - Project Hosting on Google Code</title> <pre> &quot;*all*&quot; means all  not just all in that folder  which is what it should do (and   which is much more useful if you have lots of bookmarks). Win Chrome   implements the desired behaviour. </pre>