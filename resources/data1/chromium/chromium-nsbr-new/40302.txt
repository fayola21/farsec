<title>Issue 40302 -   chromium -    multiple tabs with same http-authorization should only require one username/password entry - Project Hosting on Google Code</title>   1. open multiple tabs  with the same website  which requires http-  authorization  2. close the browser  with the setting 'Reopen the pages that were open   last'  3. open the browser      The user is only prompted to enter credentials once  as the newest release   of Firefox behaves.      The user is prompted to enter credentials for each tab  after each tab is   manually switched to.    <b>Please provide