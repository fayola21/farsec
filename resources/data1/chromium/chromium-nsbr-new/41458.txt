<title>Issue 41458 -   chromium -    Scroll bars do not obey the system settings - Project Hosting on Google Code</title> <pre> Chrome Version       : 5.0.375.6 dev  OS version               : OS X 10.6.3      1. Change system appearance to Graphite instead of the default Blue  2. Change the scrollbar arrows to the top and the bottom  3. Use Chromium normally      Scroll bar appearance would obey the system wide settings.      In select tabs  the scollbars change at random back to the default blue with the arrows at the   bottom of the scrollbar.  This does not happen in any other application on the system. </pre>