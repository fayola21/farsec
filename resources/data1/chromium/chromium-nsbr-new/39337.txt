<title>Issue 39337 -   chromium -    window.location.reload() doesn't reload background page - Project Hosting on Google Code</title> <pre> Create an extension with a background page. In that background page  do   something like log the current time via console.log. Then open the js console   for the background page and call window.location.reload(). The page should   reload.   </pre>