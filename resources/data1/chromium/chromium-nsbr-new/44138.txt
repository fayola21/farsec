<title>Issue 44138 -   chromium -    Dragging URL with linebreak to tab bar truncates URL - Project Hosting on Google Code</title> <pre>   1. Select all the text of a link that contains a line break (e.g.  almost any link in our bug tracker :P )  2. Drag it to the tab strip to create a new tab    Instead of stripping the whitespace  like we would in pasting to the omnibox  we truncate at the line   break and open a non-functional partial link. </pre>