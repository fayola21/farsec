<title>Issue 25272 -   chromium -    Cleanup: make sure we use appropriate types for PIDs - Project Hosting on Google Code</title> <pre> Probably we shouldn't use generic int's for PIDs. Instead  we should use   base::ProcessId or the appropriate native type when calling native functions   (which rarely want actual int's for PIDs).    I've made a start on it  but there are more pressing things.... </pre>