<title>Issue 24237 -   chromium -    Removing event listener from inside listener prevents other listeners from firing - Project Hosting on Google Code</title>   1. Attach two event listeners to the same event.  2. Inside the handler for one  remove it as a listener.      The second listener should still fire.      The second listener does not fire. It *does* fire for subsequent events.    <b>Please provide