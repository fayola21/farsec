<title>Issue 39895 -   chromium -    Simplify GLES2CmdDecoder destruction - Project Hosting on Google Code</title> <pre> brettw points out that explicit calls to methods like Destroy() (this   example taken from gpu_processor_linux.cc) are error-prone:      void GPUProcessor::Destroy() {      // Destroy decoder if initialized.      if (decoder_.get()) {        GLXContextWrapper *context = decoder_-&gt;context();        decoder_-&gt;Destroy();        decoder_-&gt;set_context_wrapper(NULL);        delete context;        decoder_.reset();      }      ...    }    Brett's suggestion is to figure out a way to push the cleanup code into the   GLES2CmdDecoder's destructor so that the call to decoder_.reset() does   everything necessary.   </pre>