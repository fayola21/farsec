Use X-Forwarded-For header for getting the IP address of the client in WebClientInfo    When using Apache HTTP Server's ProxyPass to forward to other HTTP Servers  such as Jetty  the client's ip address defaults to the server that forwarded the request  usually being 127...   