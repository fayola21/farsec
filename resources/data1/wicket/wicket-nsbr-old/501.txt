JavaScript comment stripping doesn't handle regexps    The following line is crippled when using javascript comment stripping(from jquery.js) // Handle the common XPath // expression if ( !t.indexOf('//') ) {it will be result in if ( !t.indexOf('the // inside the string quotes are taken as a comment instead of as a string...   