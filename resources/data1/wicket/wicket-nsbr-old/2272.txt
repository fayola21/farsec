open/close div tags are rendered erroneously    Div tags are rendered erroneously if written as an open/close tag.&lt;div/&gt; is rendered as &lt;div&gt; with the closing tag appended at the end of page in wicket-1.4-rc4  while &lt;div&gt;&lt;/div&gt; is rendered correct. Div as open/close tag works fine in wicket-1.4-rc2 even though it gets rendered as separate open and close tags.   