'set schema sys' followed by 'show tables' does not show tables in sys schema    If you create a new database in IJ  followed by 'set schema sys' and then 'show tables'  ij shows no tables in the schema. This is because ij's show tables command only shows system tables if a schema is provided.   