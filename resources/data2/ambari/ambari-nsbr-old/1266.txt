Agent checks packages as part of host check but doesn't tell which ones are needed or conflicting    Agent checks packages as part of host check but doesn't tell which ones are needed or conflicting   